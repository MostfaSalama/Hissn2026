import React, { useState, useMemo, useEffect, useRef } from 'react';
import {
  Sun, Moon, RotateCcw, ChevronRight, ChevronLeft, Check, Sparkles,
  Flame, X, BadgeCheck, Volume2, VolumeX, Trophy, Zap, BookOpen, ChevronDown, Share2, Calendar,
} from 'lucide-react';

/* ------------------------------------------------------------------ */
/* Data — أذكار الصباح والمساء مع حكم كل ذكر                           */
/* h = { rule, narrator, scholar, source, page, takhrij, note }        */
/* open: true => ذكر بعدّاد مفتوح                                       */
/* ------------------------------------------------------------------ */

const H = (rule, narrator, scholar, source, page, takhrij, note) => ({ rule, narrator, scholar, source, page, takhrij, note });

const QURAN = (note) => H('قرآن كريم', '', '', '', '', '', note);

const MORNING = [
  {
    t: 'أَصْـبَحْنا وَأَصْـبَحَ المُـلْكُ للهِ وَالحَمدُ للهِ، لا إلهَ إلاّ اللّهُ وَحدَهُ لا شَريكَ لهُ، لهُ المُـلكُ ولهُ الحَمْـد، وهُوَ على كلّ شَيءٍ قدير، رَبِّ أسْـأَلُـكَ خَـيرَ ما في هـذا اليوم وَخَـيرَ ما بَعْـدَه، وَأَعـوذُ بِكَ مِنْ شَـرِّ ما في هـذا اليوم وَشَرِّ ما بَعْـدَه، رَبِّ أَعـوذُ بِكَ مِنَ الْكَسَـلِ وَسـوءِ الْكِـبَر، رَبِّ أَعـوذُ بِكَ مِنْ عَـذابٍ في النّـارِ وَعَـذابٍ في القَـبْر',
    n: 1,
    h: H('إسناده صحيح على شرط مسلم', 'عبدالله بن مسعود', 'شعيب الأرناؤوط', 'تخريج صحيح ابن حبان', '963', 'أخرجه مسلم (2723) باختلاف يسير'),
  },
  {
    t: '﴿ اللَّهُ لَا إِلَٰهَ إِلَّا هُوَ الْحَيُّ الْقَيُّومُ ۚ لَا تَأْخُذُهُ سِنَةٌ وَلَا نَوْمٌ ۚ لَّهُ مَا فِي السَّمَاوَاتِ وَمَا فِي الْأَرْضِ ۗ مَن ذَا الَّذِي يَشْفَعُ عِندَهُ إِلَّا بِإِذْنِهِ ۚ يَعْلَمُ مَا بَيْنَ أَيْدِيهِمْ وَمَا خَلْفَهُمْ ۖ وَلَا يُحِيطُونَ بِشَيْءٍ مِّنْ عِلْمِهِ إِلَّا بِمَا شَاءَ ۚ وَسِعَ كُرْسِيُّهُ السَّمَاوَاتِ وَالْأَرْضَ ۖ وَلَا يَئُودُهُ حِفْظُهُمَا ۚ وَهُوَ الْعَلِيُّ الْعَظِيمُ ﴾ [البقرة: 255]',
    intro: 'أعوذ بالله من الشيطان الرجيم',
    s: 'آية الكرسي — البقرة 255', n: 1,
    v: 'من قالها حين يصبح أُجير من الجنّ حتى يمسي، ومن قالها حين يمسي أُجير من الجنّ حتى يصبح.',
    h: H('خلاصة حكم المحدث: هذه زيادة شاذة', 'أُبيّ بن كعب', 'الألباني', 'السلسلة الصحيحة', '7/743', 'أخرجه النسائي في ((الكبرى)) (10731)، وابن حبان (417)، وابن أبي الدنيا في ((الهواتف)) (174) بنحوه تامًا.'),
  },
  {
    t: '﴿ قُلۡ هُوَ ٱللَّهُ أَحَدٌ (1) ٱللَّهُ ٱلصَّمَدُ (2) لَمۡ يَلِدۡ وَلَمۡ يُولَدۡ (3) وَلَمۡ يَكُن لَّهُۥ كُفُوًا أَحَدُۢ (4) ﴾',
    intro: 'بسم الله الرحمن الرحيم',
    s: 'سورة الإخلاص', n: 3,
    v: 'من قالها حين يصبح وحين يمسي كفته من كل شيء (مع المعوذتين).',
    h: H('حسن صحيح', 'عبدالله بن خبيب', 'الألباني', 'هداية الرواة', '2104', 'أخرجه أبو داود (5082)، والترمذي (3575)، وابن أبي عاصم في ((الآحاد والمثاني)) (2572) واللفظ لهم، والنسائي (5428) بنحوه.'),
  },
  {
    t: '﴿ قُلۡ أَعُوذُ بِرَبِّ ٱلۡفَلَقِ (1) مِن شَرِّ مَا خَلَقَ (2) وَمِن شَرِّ غَاسِقٍ إِذَا وَقَبَ (3) وَمِن شَرِّ ٱلنَّفَّٰثَٰتِ فِي ٱلۡعُقَدِ (4) وَمِن شَرِّ حَاسِدٍ إِذَا حَسَدَ (5) ﴾',
    intro: 'بسم الله الرحمن الرحيم',
    s: 'سورة الفلق', n: 3,
    h: QURAN('قل أعوذ برب الفلق والمعوذتان: من قالهما مع الإخلاص ثلاثًا كفته من كل شيء.'),
  },
  {
    t: '﴿ قُلۡ أَعُوذُ بِرَبِّ ٱلنَّاسِ (1) مَلِكِ ٱلنَّاسِ (2) إِلَٰهِ ٱلنَّاسِ (3) مِن شَرِّ ٱلۡوَسۡوَاسِ ٱلۡخَنَّاسِ (4) ٱلَّذِي يُوَسۡوِسُ فِي صُدُورِ ٱلنَّاسِ (5) مِنَ ٱلۡجِنَّةِ وَٱلنَّاسِ (6) ﴾',
    intro: 'بسم الله الرحمن الرحيم',
    s: 'سورة الناس', n: 3,
    h: QURAN('قل أعوذ برب الناس: خاتمة المعوذتين اللتين قال فيهما النبي ﷺ: «كفتاه من كل شيء».'),
  },
  {
    t: 'اللّهـمَّ أَنْتَ رَبِّـي لا إلهَ إلاّ أَنْتَ، خَلَقْتَنـي وَأَنا عَبْـدُك، وَأَنا عَلـى عَهْـدِكَ وَوَعْـدِكَ ما اسْتَـطَعْـت، أَعـوذُ بِكَ مِنْ شَـرِّ ما صَنَـعْت، أَبـوءُ لَـكَ بِنِعْـمَتِـكَ عَلَـيَّ وَأَبـوءُ بِذَنْـبي فَاغْـفِرْ لي فَإِنَّـهُ لا يَغْـفِرُ الذُّنـوبَ إِلاّ أَنْتَ',
    s: 'سيد الاستغفار', n: 1,
    v: 'من قالها موقنًا بها حين يمسي فمات من ليلته دخل الجنة، وكذلك حين يصبح.',
    h: H('[صحيح]', 'شداد بن أوس', 'البخاري', 'صحيح البخاري', '6306', 'من أفراد البخاري على مسلم', 'مَن قالها من النهار موقنًا بها، فمات من يومه قبل أن يمسي، فهو من أهل الجنة، ومَن قالها من الليل وهو موقن بها، فمات قبل أن يصبح، فهو من أهل الجنة.'),
  },
  {
    t: 'رَضيـتُ بِاللهِ رَبَّـاً، وَبِالإسْلامِ ديـناً، وَبِمُحَـمَّدٍ صلى الله عليه وسلم نَبِيّـاً',
    n: 3,
    v: 'من قالها حين يصبح وحين يمسي كان حقًا على الله أن يُرضيه يوم القيامة.',
    h: H('صحيح', 'أبو سعيد الخدري', 'الألباني', 'صحيح أبي داود', '1529', 'أخرجه أبو داود (1529)، ومسلم (1884)، والنسائي في ((السنن الكبرى)) (9748) بلفظه.', 'من قال رضيت بالله ربًا وبالإسلام دينًا وبمحمد ﷺ رسولًا وجبت له الجنة.'),
  },
  {
    t: 'اللّهُـمَّ إِنِّـي أَصْبَـحْتُ أُشْـهِدُك، وَأُشْـهِدُ حَمَلَـةَ عَـرْشِـك، وَمَلَائِكَتَكَ، وَجَمـيعَ خَلْـقِك، أَنَّـكَ أَنْـتَ اللهُ لا إلهَ إلاّ أَنْـتَ وَحْـدَكَ لا شَريكَ لَـك، وَأَنَّ مُحَمّـداً عَبْـدُكَ وَرَسـولُـك',
    n: 4,
    v: 'من قالها أعتقه الله من النار.',
    h: H('إسناده حسن', 'أنس بن مالك', 'ابن باز', 'مجموع فتاوى ابن باز', '26/29', 'أخرجه أبو داود (5069)، والنسائي في ((السنن الكبرى)) (9837) باختلاف يسير.'),
  },
  {
    t: 'حَسْبِـيَ اللّهُ لا إلهَ إلاّ هُوَ عَلَـيهِ تَوَكَّـلتُ وَهُوَ رَبُّ العَرْشِ العَظـيم',
    n: 7,
    v: 'من قالها كفاه الله ما أهمّه من أمر الدنيا والآخرة.',
    h: H('ورد في حديث رواه ابن السني مرفوعًا وأبو داود موقوفًا: من قال حين يصبح وحين يمسي: «حسبي الله لا إله إلا هو عليه توكلت وهو رب العرش العظيم» سبع مرات، كفاه الله ما أهمه من أمر الدنيا والآخرة. صحح إسناده شعيب الأرنؤوط في تحقيق زاد المعاد.', '', '', '', '', '', 'وأما «حسبي الله ونعم الوكيل» فقد وردت في عدة أحاديث، منها ما في صحيح البخاري: كان آخر قول إبراهيم حين أُلقي في النار: حسبي الله ونعم الوكيل. ولم نقف على رواية فيها أنها تُقال سبع مرات. — موقع إسلام ويب، رقم الفتوى: 126233'),
  },
  {
    t: 'بِسـمِ اللهِ الذي لا يَضُـرُّ مَعَ اسمِـهِ شَيءٌ في الأرْضِ وَلا في السّمـاءِ وَهـوَ السّمـيعُ العَلـيم',
    n: 3,
    v: 'لم يضرّه من الله شيء.',
    h: H('إسناده صحيح', 'عثمان بن عفان', 'أحمد شاكر', 'تخريج المسند لشاكر', '2/3', 'أخرجه أبو داود (5088)، والترمذي (3388)، والنسائي في ((السنن الكبرى)) (9843)، وابن ماجه (3869)، وأحمد (446) باختلاف يسير، وعبدالله بن أحمد في ((زوائد المسند)) (528) واللفظ له.'),
  },
  {
    t: 'اللّهُـمَّ بِكَ أَصْـبَحْنا وَبِكَ أَمْسَـينا، وَبِكَ نَحْـيا وَبِكَ نَمُـوتُ وَإِلَـيْكَ النُّـشُور',
    n: 1,
    h: H('حسن صحيح', 'أبو هريرة', 'الألباني', 'الكلم الطيب', '20', 'أخرجه الترمذي (3391)'),
  },
  {
    t: 'أَصْبَـحْـنا عَلَى فِطْرَةِ الإسْلاَمِ، وَعَلَى كَلِمَةِ الإِخْلاَصِ، وَعَلَى دِينِ نَبِيِّنَا مُحَمَّدٍ صَلَّى اللهُ عَلَيْهِ وَسَلَّمَ، وَعَلَى مِلَّةِ أَبِينَا إبْرَاهِيمَ حَنِيفاً مُسْلِماً وَمَا كَانَ مِنَ المُشْرِكِينَ',
    n: 1,
    h: H('صحيح', 'عبدالرحمن بن أبزى', 'شعيب الأرناؤوط', 'تخريج المسند لشعيب', '15367', 'أخرجه النسائي في ((السنن الكبرى)) (9829)، وأحمد (15367) واللفظ له'),
  },
  {
    t: 'سُبْحـانَ اللهِ وَبِحَمْـدِهِ عَدَدَ خَلْـقِه، وَرِضـا نَفْسِـه، وَزِنَـةَ عَـرْشِـه، وَمِـدادَ كَلِمـاتِـه',
    n: 3,
    h: H('سكت عنه [وقد قال في رسالته لأهل مكة: كل ما سكت عنه فهو صالح]', 'عبدالله بن عباس', 'أبو داود', 'سنن أبي داود', '1503', 'أخرجه البيهقي في ((الدعوات الكبير)) (127) باختلاف يسير، وأحمد (2334)، والنسائي في ((الكبرى)) (9917) بنحوه.', 'خرج رسول الله ﷺ من عند جويرية رضي الله عنها -وكان اسمها برة فحول اسمها- فخرج وهي في مصلاها، ورجع وهي في مصلاها، فقال: «لم تزالي في مصلاك هذا؟!» قالت: نعم. قال: «قد قلت بعدك أربع كلمات ثلاث مرات لو وُزنت بما قلت لوزنتهن: سبحان الله وبحمده عدد خلقه، ورضا نفسه، وزنة عرشه، ومداد كلماته».'),
  },
  {
    t: 'اللّهُـمَّ عافِـني في بَدَنـي، اللّهُـمَّ عافِـني في سَمْـعي، اللّهُـمَّ عافِـني في بَصَـري، لا إلهَ إلاّ أَنْـتَ',
    n: 3,
    h: H('إسناده حسن', 'أبو بكرة نفيع بن الحارث', 'الألباني', 'صحيح أبي داود', '5090', 'أخرجه أبو داود (5090)، وأحمد (20430) والنسائي في ((السنن الكبرى)) (9766) جميعا بلفظه مطولا.'),
  },
  {
    t: 'اللّهُـمَّ إِنّـي أَعـوذُ بِكَ مِنَ الْكُـفر، وَالفَـقْر، وَأَعـوذُ بِكَ مِنْ عَذابِ القَـبْر، لا إلهَ إلاّ أَنْـتَ',
    n: 3,
    h: H('إسناده حسن', 'أبو بكرة نفيع بن الحارث', 'الألباني', 'صحيح أبي داود', '5090', 'أخرجه أبو داود (5090)، وأحمد (20430) والنسائي في ((السنن الكبرى)) (9766) جميعا بلفظه مطولا.'),
  },
  {
    t: 'اللّهُـمَّ إِنِّـي أسْـأَلُـكَ العَـفْوَ وَالعـافِـيةَ في الدُّنْـيا وَالآخِـرَة، اللّهُـمَّ إِنِّـي أسْـأَلُـكَ العَـفْوَ وَالعـافِـيةَ في ديني وَدُنْـيايَ وَأهْـلي وَمالـي، اللّهُـمَّ اسْتُـرْ عـوْراتي وَآمِـنْ رَوْعاتـي، اللّهُـمَّ احْفَظْـني مِن بَـينِ يَدَيَّ وَمِن خَلْفـي وَعَن يَمـيني وَعَن شِمـالي، وَمِن فَوْقـي، وَأَعـوذُ بِعَظَمَـتِكَ أَن أُغْـتالَ مِن تَحْتـي',
    n: 1,
    h: H('إسناده صحيح', 'عبدالله بن عمر', 'الألباني', 'الكلم الطيب', '27', 'أخرجه أبو داود (5074)، والنسائي (5530)، وابن ماجه (3871)، وأحمد (4785) واللفظ له.'),
  },
  {
    t: 'يَا حَيُّ يَا قيُّومُ بِرَحْمَتِكَ أسْتَغِيثُ، أصْلِحْ لِي شَأنِي كُلَّهُ وَلاَ تَكِلْنِي إلَى نَفْسِي طَـرْفَةَ عَيْنٍ',
    n: 3,
    h: H('حسن', 'أنس بن مالك', 'الألباني', 'صحيح الترغيب', '661', 'أخرجه النسائي في ((السنن الكبرى)) (10405)، والبزار (6368)، وابن السني في ((عمل اليوم والليلة)) (48)'),
  },
  {
    t: 'اللّهُـمَّ عالِـمَ الغَـيْبِ وَالشّـهادَةِ فاطِـرَ السّماواتِ وَالأرْضِ رَبَّ كـلِّ شَـيءٍ وَمَليـكَه، أَشْهَـدُ أَنْ لا إِلـهَ إِلاّ أَنْت، أَعـوذُ بِكَ مِن شَـرِّ نَفْسـي وَمِن شَـرِّ الشَّيْـطانِ وَشِرْكِهِ، وَأَنْ أَقْتَـرِفَ عَلـى نَفْسـي سوءاً أَوْ أَجُـرَّهُ إِلـى مُسْـلِم',
    n: 1,
    h: H('صحيح', 'عبدالله بن عمرو', 'الألباني', 'صحيح الجامع', '7813', 'أخرجه الترمذي (3529)، وأحمد (6597) باختلاف يسير، وابن عساكر في ((معجم الشيوخ)) (1246) واللفظ له', 'يا أبا بكرٍ! قل: اللهم فاطر السماوات والأرض، عالم الغيب والشهادة، لا إله إلا أنت، رب كل شيء ومليكه، أعوذ بك من شر نفسي، ومن شر الشيطان وشِرْكه، وأن أقترف على نفسي سوءًا أو أجرّه إلى مسلم'),
  },
  {
    t: 'أَعـوذُ بِكَلِمـاتِ اللّهِ التّـامّـاتِ مِنْ شَـرِّ ما خَلَـق',
    n: 3,
    h: H('صحيح', 'أبو هريرة', 'ابن حجر العسقلاني', 'الفتوحات الربانية', '3/95', 'أخرجه الترمذي (3604)، وأحمد (7898) باختلاف يسير، وأخرجه مسلم (2709) مختصراً بلفظ مقارب', 'مَن قال إذا أمسى ثلاث مرات: أعوذ بكلمات الله التامات من شر ما خلق، لم تضرّه حُمَة تلك الليلة. قال: فكان أهلنا قد تعلموها، فكانوا يقولونها كل ليلة، فلُدغت جارية منهم فلم تجد لها ألمًا'),
  },
  {
    t: 'اللَّهُمَّ إِنَّا نَعُوذُ بِكَ مِنْ أَنْ نُشْرِكَ بِكَ شَيْئًا نَعْلَمُهُ، وَنَسْتَغْفِرُكَ لِمَا لَا نَعْلَمُهُ',
    n: 3,
    h: H('صحيح', 'معقل بن يسار', 'الألباني', 'صحيح الأدب المفرد', '551', 'أخرجه أبو يعلى (61)، وابن بطة في ((الإبانة الصغرى)) (982) باختلاف يسير.', 'يا أبا بكرٍ، لَلشِّركُ فيكم أخفى من دبيب النمل، والذي نفسي بيده لَلشِّرك أخفى من دبيب النمل، ألا أدلُّك على شيء إذا فعلته ذهب عنك قليله وكثيره؟ قل: اللهم إني أعوذ بك أن أُشرك بك وأنا أعلم، وأستغفرك لما لا أعلم'),
  },
  {
    t: 'اللَّهُمَّ إِنِّي أَعُوذُ بِكَ مِنْ الْهَمِّ وَالْحَزَنِ، وَأَعُوذُ بِكَ مِنْ الْعَجْزِ وَالْكَسَلِ، وَأَعُوذُ بِكَ مِنْ الْجُبْنِ وَالْبُخْلِ، وَأَعُوذُ بِكَ مِنْ غَلَبَةِ الدَّيْنِ وَقَهْرِ الرِّجَالِ',
    n: 3,
    h: H('صحيح', 'أبو سعيد الخدري', 'السيوطي', 'الجامع الصغير', '2864', 'أخرجه أبو داود (1555)، والبيهقي في ((الدعوات الكبير)) (305)، والمزي في ((تهذيب الكمال)) (23/105) جميعا بلفظه، وفي أوله قصة.'),
  },
  {
    t: 'أسْتَغْفِرُ اللهَ العَظِيمَ الَّذِي لاَ إلَهَ إلاَّ هُوَ، الحَيَّ القَيُّومَ، وَأتُوبُ إلَيهِ',
    n: 3,
    h: H('صحيح', 'عبدالله بن مسعود', 'الألباني', 'صحيح الترغيب', '1623', 'أخرجه الطبراني في ((المعجم الكبير)) (9/103) (8541)، واللفظ له، والحاكم في ((المستدرك على الصحيحين)) (1901)، باختلاف يسير.', 'من قالها ثلاثًا غُفر له وإن كان فرَّ من الزحف.'),
  },
  {
    t: 'اللَّهُمَّ إِنِّي أَسْأَلُكَ عِلْمًا نَافِعًا، وَرِزْقًا طَيِّبًا، وَعَمَلًا مُتَقَبَّلًا',
    n: 1,
    h: H('حسن', 'أم سلمة أم المؤمنين', 'ابن حجر العسقلاني', 'نتائج الأفكار', '2/411', 'أخرجه عبد بن حميد (1533) واللفظ له، وأخرجه ابن ماجه (925)، وأحمد (26602) باختلاف يسير', 'أن النبي ﷺ كان إذا أصبح قال: اللهم إني أسألك علمًا نافعًا، ورزقًا طيبًا، وعملًا متقبلًا'),
  },
  {
    t: 'لَا إلَه إلّا اللهُ وَحْدَهُ لَا شَرِيكَ لَهُ، لَهُ الْمُلْكُ وَلَهُ الْحَمْدُ وَهُوَ عَلَى كُلِّ شَيْءِ قَدِيرِ',
    n: 100,
    v: 'كانت له عدل عشر رقاب، وكُتبت له مئة حسنة، ومُحيت عنه مئة سيئة، وكانت له حِرزًا من الشيطان.',
    h: H('صحيح', 'أبو هريرة', 'الألباني', 'صحيح ابن ماجه', '3079', 'أخرجه البخاري (3293)، ومسلم (2691)، والترمذي (3468)، والنسائي في ((السنن الكبرى)) (9853)، وابن ماجه (3798) واللفظ له، وأحمد (8008)', 'من قال في يوم مائة مرة: لا إله إلا الله وحده لا شريك له، له الملك وله الحمد وهو على كل شيء قدير، كان له عدل عشر رقاب، وكُتبت له مائة حسنة، ومُحيت عنه مائة سيئة، وكنّ له حرزًا من الشيطان سائر يومه إلى الليل، ولم يأتِ أحد بأفضل مما أتى به إلا من قال أكثر'),
  },
  /* أذكار العدد المفتوح */
  {
    t: 'اللَّهُمَّ صَلِّ وَسَلِّمْ وَبَارِكْ على نَبِيِّنَا مُحمَّد',
    s: 'الصلاة على النبي ﷺ', open: true,
    v: 'من صلّى عليّ صلاةً صلّى الله عليه بها عشرًا.',
    h: H('ثابت — من صلّى عليّ صلاةً صلّى الله عليه بها عشرًا', 'أبو هريرة', 'مسلم', 'صحيح مسلم', '408', 'أخرجه مسلم (408)، وأبو داود (1530)، والترمذي (485)'),
  },
  {
    t: 'سُبْحـانَ اللهِ وَبِحَمْـدِهِ',
    s: 'تسبيح', open: true,
  },
  {
    t: 'أسْتَغْفِرُ اللهَ وَأتُوبُ إلَيْهِ',
    s: 'استغفار', open: true,
  },
];

const EVENING = [
  {
    t: 'أَمْسَيْـنا وَأَمْسـى المـلكُ للهِ وَالحَمدُ للهِ، لا إلهَ إلاّ اللّهُ وَحدَهُ لا شريكَ لهُ، لهُ المُـلكُ ولهُ الحَمْـد، وهُوَ على كلّ شَيءٍ قدير، رَبِّ أسْـأَلُـكَ خَـيرَ ما في هـذهِ اللَّـيْلَةِ وَخَـيرَ ما بَعْـدَهـا، وَأَعـوذُ بِكَ مِنْ شَـرِّ ما في هـذهِ اللَّـيْلةِ وَشَرِّ ما بَعْـدَهـا، رَبِّ أَعـوذُ بِكَ مِنَ الْكَسَـلِ وَسـوءِ الْكِـبَر، رَبِّ أَعـوذُ بِكَ مِنْ عَـذابٍ في النّـارِ وَعَـذابٍ في القَـبْر',
    n: 1,
    h: H('[صحيح]', 'عبدالله بن مسعود', 'مسلم', 'صحيح مسلم', '2723', 'من أفراد مسلم على البخاري'),
  },
  {
    t: '﴿ اللَّهُ لَا إِلَٰهَ إِلَّا هُوَ الْحَيُّ الْقَيُّومُ ۚ لَا تَأْخُذُهُ سِنَةٌ وَلَا نَوْمٌ ۚ لَّهُ مَا فِي السَّمَاوَاتِ وَمَا فِي الْأَرْضِ ۗ مَن ذَا الَّذِي يَشْفَعُ عِندَهُ إِلَّا بِإِذْنِهِ ۚ يَعْلَمُ مَا بَيْنَ أَيْدِيهِمْ وَمَا خَلْفَهُمْ ۖ وَلَا يُحِيطُونَ بِشَيْءٍ مِّنْ عِلْمِهِ إِلَّا بِمَا شَاءَ ۚ وَسِعَ كُرْسِيُّهُ السَّمَاوَاتِ وَالْأَرْضَ ۖ وَلَا يَئُودُهُ حِفْظُهُمَا ۚ وَهُوَ الْعَلِيُّ الْعَظِيمُ ﴾ [البقرة: 255]',
    intro: 'أعوذ بالله من الشيطان الرجيم',
    s: 'آية الكرسي — البقرة 255', n: 1,
    v: 'من قالها حين يصبح أُجير من الجنّ حتى يمسي، ومن قالها حين يمسي أُجير من الجنّ حتى يصبح.',
    h: H('خلاصة حكم المحدث: هذه زيادة شاذة', 'أُبيّ بن كعب', 'الألباني', 'السلسلة الصحيحة', '7/743', 'أخرجه النسائي في ((الكبرى)) (10731)، وابن حبان (417)، وابن أبي الدنيا في ((الهواتف)) (174) بنحوه تامًا.'),
  },
  {
    t: '﴿ ءَامَنَ ٱلرَّسُولُ بِمَآ أُنزِلَ إِلَيۡهِ مِن رَّبِّهِۦ وَٱلۡمُؤۡمِنُونَۚ كُلٌّ ءَامَنَ بِٱللَّهِ وَمَلَٰٓئِكَتِهِۦ وَكُتُبِهِۦ وَرُسُلِهِۦ لَا نُفَرِّقُ بَيۡنَ أَحَدٖ مِّن رُّسُلِهِۦۚ وَقَالُواْ سَمِعۡنَا وَأَطَعۡنَاۖ غُفۡرَانَكَ رَبَّنَا وَإِلَيۡكَ ٱلۡمَصِيرُ (285) لَا يُكَلِّفُ ٱللَّهُ نَفۡسًا إِلَّا وُسۡعَهَاۚ لَهَا مَا كَسَبَتۡ وَعَلَيۡهَا مَا ٱكۡتَسَبَتۡۗ رَبَّنَا لَا تُؤَاخِذۡنَآ إِن نَّسِينَآ أَوۡ أَخۡطَأۡنَاۚ رَبَّنَا وَلَا تَحۡمِلۡ عَلَيۡنَآ إِصۡرٗا كَمَا حَمَلۡتَهُۥ عَلَى ٱلَّذِينَ مِن قَبۡلِنَاۚ رَبَّنَا وَلَا تُحَمِّلۡنَا مَا لَا طَاقَةَ لَنَا بِهِۦۖ وَٱعۡفُ عَنَّا وَٱغۡفِرۡ لَنَا وَٱرۡحَمۡنَآ أَنتَ مَوۡلَىٰنَا فَٱنصُرۡنَا عَلَى ٱلۡقَوۡمِ ٱلۡكَٰفِرِينَ (286) ﴾ [البقرة: 285 - 286]',
    intro: 'أعوذ بالله من الشيطان الرجيم',
    s: 'آخر آيتين من سورة البقرة', n: 1,
    v: 'من قرأ بالآيتين من آخر سورة البقرة في ليلة كفتاه.',
    h: QURAN('من قرأ بالآيتين من آخر سورة البقرة في ليلة كفتاه — أخرجه البخاري ومسلم'),
  },
  {
    t: '﴿ قُلۡ هُوَ ٱللَّهُ أَحَدٌ (1) ٱللَّهُ ٱلصَّمَدُ (2) لَمۡ يَلِدۡ وَلَمۡ يُولَدۡ (3) وَلَمۡ يَكُن لَّهُۥ كُفُوًا أَحَدُۢ (4) ﴾',
    intro: 'بسم الله الرحمن الرحيم',
    s: 'سورة الإخلاص', n: 3,
    v: 'من قالها حين يصبح وحين يمسي كفته من كل شيء (مع المعوذتين).',
    h: H('حسن صحيح', 'عبدالله بن خبيب', 'الألباني', 'هداية الرواة', '2104', 'أخرجه أبو داود (5082)، والترمذي (3575)، وابن أبي عاصم في ((الآحاد والمثاني)) (2572) واللفظ لهم، والنسائي (5428) بنحوه.'),
  },
  {
    t: '﴿ قُلۡ أَعُوذُ بِرَبِّ ٱلۡفَلَقِ (1) مِن شَرِّ مَا خَلَقَ (2) وَمِن شَرِّ غَاسِقٍ إِذَا وَقَبَ (3) وَمِن شَرِّ ٱلنَّفَّٰثَٰتِ فِي ٱلۡعُقَدِ (4) وَمِن شَرِّ حَاسِدٍ إِذَا حَسَدَ (5) ﴾',
    intro: 'بسم الله الرحمن الرحيم',
    s: 'سورة الفلق', n: 3,
    h: QURAN('قل أعوذ برب الفلق والمعوذتان: من قالهما مع الإخلاص ثلاثًا كفته من كل شيء.'),
  },
  {
    t: '﴿ قُلۡ أَعُوذُ بِرَبِّ ٱلنَّاسِ (1) مَلِكِ ٱلنَّاسِ (2) إِلَٰهِ ٱلنَّاسِ (3) مِن شَرِّ ٱلۡوَسۡوَاسِ ٱلۡخَنَّاسِ (4) ٱلَّذِي يُوَسۡوِسُ فِي صُدُورِ ٱلنَّاسِ (5) مِنَ ٱلۡجِنَّةِ وَٱلنَّاسِ (6) ﴾',
    intro: 'بسم الله الرحمن الرحيم',
    s: 'سورة الناس', n: 3,
    h: QURAN('قل أعوذ برب الناس: خاتمة المعوذتين اللتين قال فيهما النبي ﷺ: «كفتاه من كل شيء».'),
  },
  {
    t: 'اللّهـمَّ أَنْتَ رَبِّـي لا إلهَ إلاّ أَنْتَ، خَلَقْتَنـي وَأَنا عَبْـدُك، وَأَنا عَلـى عَهْـدِكَ وَوَعْـدِكَ ما اسْتَـطَعْـت، أَعـوذُ بِكَ مِنْ شَـرِّ ما صَنَـعْت، أَبـوءُ لَـكَ بِنِعْـمَتِـكَ عَلَـيَّ وَأَبـوءُ بِذَنْـبي فَاغْـفِرْ لي فَإِنَّـهُ لا يَغْـفِرُ الذُّنـوبَ إِلاّ أَنْتَ',
    s: 'سيد الاستغفار', n: 1,
    v: 'من قالها موقنًا بها حين يمسي فمات من ليلته دخل الجنة، وكذلك حين يصبح.',
    h: H('[صحيح]', 'شداد بن أوس', 'البخاري', 'صحيح البخاري', '6306', 'من أفراد البخاري على مسلم', 'مَن قالها من النهار موقنًا بها، فمات من يومه قبل أن يمسي، فهو من أهل الجنة، ومَن قالها من الليل وهو موقن بها، فمات قبل أن يصبح، فهو من أهل الجنة.'),
  },
  {
    t: 'رَضيـتُ بِاللهِ رَبَّـاً، وَبِالإسْلامِ ديـناً، وَبِمُحَـمَّدٍ صلى الله عليه وسلم نَبِيّـاً',
    n: 3,
    v: 'من قالها حين يصبح وحين يمسي كان حقًا على الله أن يُرضيه يوم القيامة.',
    h: H('صحيح', 'أبو سعيد الخدري', 'الألباني', 'صحيح أبي داود', '1529', 'أخرجه أبو داود (1529)، ومسلم (1884)، والنسائي في ((السنن الكبرى)) (9748) بلفظه.', 'من قال رضيت بالله ربًا وبالإسلام دينًا وبمحمد ﷺ رسولًا وجبت له الجنة.'),
  },
  {
    t: 'اللّهُـمَّ إِنِّـي أَمسيتُ أُشْـهِدُك، وَأُشْـهِدُ حَمَلَـةَ عَـرْشِـك، وَمَلَائِكَتَكَ، وَجَمـيعَ خَلْـقِك، أَنَّـكَ أَنْـتَ اللهُ لا إلهَ إلاّ أَنْـتَ وَحْـدَكَ لا شَريكَ لَـك، وَأَنَّ مُحَمّـداً عَبْـدُكَ وَرَسـولُـك',
    n: 4,
    v: 'من قالها أعتقه الله من النار.',
    h: H('إسناده حسن', 'أنس بن مالك', 'ابن باز', 'مجموع فتاوى ابن باز', '26/29', 'أخرجه أبو داود (5069)، والنسائي في ((السنن الكبرى)) (9837) باختلاف يسير.'),
  },
  {
    t: 'حَسْبِـيَ اللّهُ لا إلهَ إلاّ هُوَ عَلَـيهِ تَوَكَّـلتُ وَهُوَ رَبُّ العَرْشِ العَظـيم',
    n: 7,
    v: 'من قالها كفاه الله ما أهمّه من أمر الدنيا والآخرة.',
    h: H('ورد في حديث رواه ابن السني مرفوعًا وأبو داود موقوفًا: من قال حين يصبح وحين يمسي: «حسبي الله لا إله إلا هو عليه توكلت وهو رب العرش العظيم» سبع مرات، كفاه الله ما أهمه من أمر الدنيا والآخرة. صحح إسناده شعيب الأرنؤوط في تحقيق زاد المعاد.', '', '', '', '', '', 'وأما «حسبي الله ونعم الوكيل» فقد وردت في عدة أحاديث، منها ما في صحيح البخاري: كان آخر قول إبراهيم حين أُلقي في النار: حسبي الله ونعم الوكيل. ولم نقف على رواية فيها أنها تُقال سبع مرات. — موقع إسلام ويب، رقم الفتوى: 126233'),
  },
  {
    t: 'بِسـمِ اللهِ الذي لا يَضُـرُّ مَعَ اسمِـهِ شَيءٌ في الأرْضِ وَلا في السّمـاءِ وَهـوَ السّمـيعُ العَلـيم',
    n: 3,
    v: 'لم يضرّه من الله شيء.',
    h: H('إسناده صحيح', 'عثمان بن عفان', 'أحمد شاكر', 'تخريج المسند لشاكر', '2/3', 'أخرجه أبو داود (5088)، والترمذي (3388)، والنسائي في ((السنن الكبرى)) (9843)، وابن ماجه (3869)، وأحمد (446) باختلاف يسير، وعبدالله بن أحمد في ((زوائد المسند)) (528) واللفظ له.'),
  },
  {
    t: 'اللّهُـمَّ بِكَ أَمْسَـينا وَبِكَ أَصْـبَحْنا، وَبِكَ نَحْـيا وَبِكَ نَمُـوتُ وَإِلَـيْكَ الْمَصِيرُ',
    n: 1,
    h: H('حسن صحيح', 'أبو هريرة', 'الألباني', 'الكلم الطيب', '20', 'أخرجه الترمذي (3391)'),
  },
  {
    t: 'أَمْسَيْنَا عَلَى فِطْرَةِ الإسْلاَمِ، وَعَلَى كَلِمَةِ الإِخْلاَصِ، وَعَلَى دِينِ نَبِيِّنَا مُحَمَّدٍ صَلَّى اللهُ عَلَيْهِ وَسَلَّمَ، وَعَلَى مِلَّةِ أَبِينَا إبْرَاهِيمَ حَنِيفاً مُسْلِماً وَمَا كَانَ مِنَ المُشْرِكِينَ',
    n: 1,
    h: H('صحيح', 'عبدالرحمن بن أبزى', 'شعيب الأرناؤوط', 'تخريج المسند لشعيب', '15367', 'أخرجه النسائي في ((السنن الكبرى)) (9829)، وأحمد (15367) واللفظ له'),
  },
  {
    t: 'سُبْحـانَ اللهِ وَبِحَمْـدِهِ عَدَدَ خَلْـقِه، وَرِضـا نَفْسِـه، وَزِنَـةَ عَـرْشِـه، وَمِـدادَ كَلِمـاتِـه',
    n: 3,
    h: H('سكت عنه [وقد قال في رسالته لأهل مكة: كل ما سكت عنه فهو صالح]', 'عبدالله بن عباس', 'أبو داود', 'سنن أبي داود', '1503', 'أخرجه البيهقي في ((الدعوات الكبير)) (127) باختلاف يسير، وأحمد (2334)، والنسائي في ((الكبرى)) (9917) بنحوه.', 'خرج رسول الله ﷺ من عند جويرية رضي الله عنها -وكان اسمها برة فحول اسمها- فخرج وهي في مصلاها، ورجع وهي في مصلاها، فقال: «لم تزالي في مصلاك هذا؟!» قالت: نعم. قال: «قد قلت بعدك أربع كلمات ثلاث مرات لو وُزنت بما قلت لوزنتهن: سبحان الله وبحمده عدد خلقه، ورضا نفسه، وزنة عرشه، ومداد كلماته».'),
  },
  {
    t: 'اللّهُـمَّ عافِـني في بَدَنـي، اللّهُـمَّ عافِـني في سَمْـعي، اللّهُـمَّ عافِـني في بَصَـري، لا إلهَ إلاّ أَنْـتَ',
    n: 3,
    h: H('إسناده حسن', 'أبو بكرة نفيع بن الحارث', 'الألباني', 'صحيح أبي داود', '5090', 'أخرجه أبو داود (5090)، وأحمد (20430) والنسائي في ((السنن الكبرى)) (9766) جميعا بلفظه مطولا.'),
  },
  {
    t: 'اللّهُـمَّ إِنّـي أَعـوذُ بِكَ مِنَ الْكُـفر، وَالفَـقْر، وَأَعـوذُ بِكَ مِنْ عَذابِ القَـبْر، لا إلهَ إلاّ أَنْـتَ',
    n: 3,
    h: H('إسناده حسن', 'أبو بكرة نفيع بن الحارث', 'الألباني', 'صحيح أبي داود', '5090', 'أخرجه أبو داود (5090)، وأحمد (20430) والنسائي في ((السنن الكبرى)) (9766) جميعا بلفظه مطولا.'),
  },
  {
    t: 'اللّهُـمَّ إِنِّـي أسْـأَلُـكَ العَـفْوَ وَالعـافِـيةَ في الدُّنْـيا وَالآخِـرَة، اللّهُـمَّ إِنِّـي أسْـأَلُـكَ العَـفْوَ وَالعـافِـيةَ في ديني وَدُنْـيايَ وَأهْـلي وَمالـي، اللّهُـمَّ اسْتُـرْ عـوْراتي وَآمِـنْ رَوْعاتـي، اللّهُـمَّ احْفَظْـني مِن بَـينِ يَدَيَّ وَمِن خَلْفـي وَعَن يَمـيني وَعَن شِمـالي، وَمِن فَوْقـي، وَأَعـوذُ بِعَظَمَـتِكَ أَن أُغْـتالَ مِن تَحْتـي',
    n: 1,
    h: H('إسناده صحيح', 'عبدالله بن عمر', 'الألباني', 'الكلم الطيب', '27', 'أخرجه أبو داود (5074)، والنسائي (5530)، وابن ماجه (3871)، وأحمد (4785) واللفظ له.'),
  },
  {
    t: 'يَا حَيُّ يَا قيُّومُ بِرَحْمَتِكَ أسْتَغِيثُ، أصْلِحْ لِي شَأنِي كُلَّهُ وَلاَ تَكِلْنِي إلَى نَفْسِي طَـرْفَةَ عَيْنٍ',
    n: 3,
    h: H('حسن', 'أنس بن مالك', 'الألباني', 'صحيح الترغيب', '661', 'أخرجه النسائي في ((السنن الكبرى)) (10405)، والبزار (6368)، وابن السني في ((عمل اليوم والليلة)) (48)'),
  },
  {
    t: 'اللّهُـمَّ عالِـمَ الغَـيْبِ وَالشّـهادَةِ فاطِـرَ السّماواتِ وَالأرْضِ رَبَّ كـلِّ شَـيءٍ وَمَليـكَه، أَشْهَـدُ أَنْ لا إِلـهَ إِلاّ أَنْت، أَعـوذُ بِكَ مِن شَـرِّ نَفْسـي وَمِن شَـرِّ الشَّيْـطانِ وَشِرْكِهِ، وَأَنْ أَقْتَـرِفَ عَلـى نَفْسـي سوءاً أَوْ أَجُـرَّهُ إِلـى مُسْـلِم',
    n: 1,
    h: H('صحيح', 'عبدالله بن عمرو', 'الألباني', 'صحيح الجامع', '7813', 'أخرجه الترمذي (3529)، وأحمد (6597) باختلاف يسير، وابن عساكر في ((معجم الشيوخ)) (1246) واللفظ له', 'يا أبا بكرٍ! قل: اللهم فاطر السماوات والأرض، عالم الغيب والشهادة، لا إله إلا أنت، رب كل شيء ومليكه، أعوذ بك من شر نفسي، ومن شر الشيطان وشِرْكه، وأن أقترف على نفسي سوءًا أو أجرّه إلى مسلم'),
  },
  {
    t: 'أَعـوذُ بِكَلِمـاتِ اللّهِ التّـامّـاتِ مِنْ شَـرِّ ما خَلَـق',
    n: 3,
    h: H('صحيح', 'أبو هريرة', 'ابن حجر العسقلاني', 'الفتوحات الربانية', '3/95', 'أخرجه الترمذي (3604)، وأحمد (7898) باختلاف يسير، وأخرجه مسلم (2709) مختصراً بلفظ مقارب', 'مَن قال إذا أمسى ثلاث مرات: أعوذ بكلمات الله التامات من شر ما خلق، لم تضرّه حُمَة تلك الليلة. قال: فكان أهلنا قد تعلموها، فكانوا يقولونها كل ليلة، فلُدغت جارية منهم فلم تجد لها ألمًا'),
  },
  {
    t: 'اللَّهُمَّ إِنَّا نَعُوذُ بِكَ مِنْ أَنْ نُشْرِكَ بِكَ شَيْئًا نَعْلَمُهُ، وَنَسْتَغْفِرُكَ لِمَا لَا نَعْلَمُهُ',
    n: 3,
    h: H('صحيح', 'معقل بن يسار', 'الألباني', 'صحيح الأدب المفرد', '551', 'أخرجه أبو يعلى (61)، وابن بطة في ((الإبانة الصغرى)) (982) باختلاف يسير.', 'يا أبا بكرٍ، لَلشِّركُ فيكم أخفى من دبيب النمل، والذي نفسي بيده لَلشِّرك أخفى من دبيب النمل، ألا أدلُّك على شيء إذا فعلته ذهب عنك قليله وكثيره؟ قل: اللهم إني أعوذ بك أن أُشرك بك وأنا أعلم، وأستغفرك لما لا أعلم'),
  },
  {
    t: 'اللَّهُمَّ إِنِّي أَعُوذُ بِكَ مِنْ الْهَمِّ وَالْحَزَنِ، وَأَعُوذُ بِكَ مِنْ الْعَجْزِ وَالْكَسَلِ، وَأَعُوذُ بِكَ مِنْ الْجُبْنِ وَالْبُخْلِ، وَأَعُوذُ بِكَ مِنْ غَلَبَةِ الدَّيْنِ وَقَهْرِ الرِّجَالِ',
    n: 3,
    h: H('صحيح', 'أبو سعيد الخدري', 'السيوطي', 'الجامع الصغير', '2864', 'أخرجه أبو داود (1555)، والبيهقي في ((الدعوات الكبير)) (305)، والمزي في ((تهذيب الكمال)) (23/105) جميعا بلفظه، وفي أوله قصة.'),
  },
  {
    t: 'أسْتَغْفِرُ اللهَ العَظِيمَ الَّذِي لاَ إلَهَ إلاَّ هُوَ، الحَيَّ القَيُّومَ، وَأتُوبُ إلَيهِ',
    n: 3,
    h: H('صحيح', 'عبدالله بن مسعود', 'الألباني', 'صحيح الترغيب', '1623', 'أخرجه الطبراني في ((المعجم الكبير)) (9/103) (8541)، واللفظ له، والحاكم في ((المستدرك على الصحيحين)) (1901)، باختلاف يسير.', 'من قالها ثلاثًا غُفر له وإن كان فرَّ من الزحف.'),
  },
  {
    t: 'لَا إلَه إلّا اللهُ وَحْدَهُ لَا شَرِيكَ لَهُ، لَهُ الْمُلْكُ وَلَهُ الْحَمْدُ وَهُوَ عَلَى كُلِّ شَيْءِ قَدِيرِ',
    n: 100,
    v: 'كانت له عدل عشر رقاب، وكُتبت له مئة حسنة، ومُحيت عنه مئة سيئة، وكانت له حِرزًا من الشيطان.',
    h: H('صحيح', 'أبو هريرة', 'الألباني', 'صحيح ابن ماجه', '3079', 'أخرجه البخاري (3293)، ومسلم (2691)، والترمذي (3468)، والنسائي في ((السنن الكبرى)) (9853)، وابن ماجه (3798) واللفظ له، وأحمد (8008)', 'من قال في يوم مائة مرة: لا إله إلا الله وحده لا شريك له، له الملك وله الحمد وهو على كل شيء قدير، كان له عدل عشر رقاب، وكُتبت له مائة حسنة، ومُحيت عنه مائة سيئة، وكنّ له حرزًا من الشيطان سائر يومه إلى الليل، ولم يأتِ أحد بأفضل مما أتى به إلا من قال أكثر'),
  },
  /* أذكار العدد المفتوح */
  {
    t: 'اللَّهُمَّ صَلِّ وَسَلِّمْ وَبَارِكْ على نَبِيِّنَا مُحمَّد',
    s: 'الصلاة على النبي ﷺ', open: true,
    v: 'من صلّى عليّ صلاةً صلّى الله عليه بها عشرًا.',
    h: H('ثابت — من صلّى عليّ صلاةً صلّى الله عليه بها عشرًا', 'أبو هريرة', 'مسلم', 'صحيح مسلم', '408', 'أخرجه مسلم (408)، وأبو داود (1530)، والترمذي (485)'),
  },
  {
    t: 'سُبْحـانَ اللهِ وَبِحَمْـدِهِ',
    s: 'تسبيح', open: true,
  },
  {
    t: 'أسْتَغْفِرُ اللهَ وَأتُوبُ إلَيْهِ',
    s: 'استغفار', open: true,
  },
];

/* ------------------------------------------------------------------ */
/* Helpers                                                             */
/* ------------------------------------------------------------------ */

const AR_DIGITS = ['٠', '١', '٢', '٣', '٤', '٥', '٦', '٧', '٨', '٩'];
const toAr = (num) => String(num).split('').map((c) => (/\d/.test(c) ? AR_DIGITS[+c] : c)).join('');

const dateStr = (off = 0) => {
  const d = new Date();
  d.setDate(d.getDate() + off);
  return `${d.getFullYear()}-${String(d.getMonth() + 1).padStart(2, '0')}-${String(d.getDate()).padStart(2, '0')}`;
};

const arcPoint = (t) => ({ x: 96 - t * 92, y: 34 - 30 * Math.sin(t * Math.PI) });

const getHistory = () => { try { return JSON.parse(localStorage.getItem('azkar_history') || '{}'); } catch (e) { return {}; } };
const saveHistory = (date, mode) => { try { const h = getHistory(); if (!h[date]) h[date] = {}; h[date][mode] = true; localStorage.setItem('azkar_history', JSON.stringify(h)); } catch (e) {} };

const gradeOf = (h, p) => {
  const rule = h.rule || '';
  const tak = h.takhrij || '';
  const txt = rule + ' ' + tak;
  if (/قرآن/.test(rule)) return { label: 'قرآن كريم', color: p.accent };
  if (/شاذ|ضعيف|منكر/.test(txt)) return { label: 'فيها مقال — يُنظر فيها', color: p.warn };
  if (/صحيح/.test(txt) && /حسن/.test(txt)) return { label: 'حسن صحيح', color: p.teal };
  if (/صحيح/.test(txt)) return { label: 'صحيح', color: p.succ };
  if (/حسن/.test(txt)) return { label: 'حسن', color: p.teal };
  return { label: 'ثابت', color: p.accent };
};

const loadState = (m) => {
  const L = m === 'morning' ? MORNING : EVENING;
  try {
    const raw = localStorage.getItem(`azkar_p_${dateStr()}_${m}`);
    if (raw) {
      const p = JSON.parse(raw);
      if (Array.isArray(p.r) && p.r.length === L.length) {
        return {
          r: p.r,
          o: Array.isArray(p.o) && p.o.length === L.length ? p.o : L.map(() => false),
          i: Math.min(Number(p.i) || 0, L.length - 1),
        };
      }
    }
  } catch (e) {}
  return { r: L.map((d) => (d.open ? null : d.n)), o: L.map(() => false), i: 0 };
};

const readDone = (m) => {
  try {
    const d = JSON.parse(localStorage.getItem('azkar_done') || '{}');
    return d[m] === dateStr();
  } catch (e) { return false; }
};

const vibrate = (ms) => { try { if (navigator.vibrate) navigator.vibrate(ms); } catch (e) {} };

function useSound() {
  const [muted, setMuted] = useState(() => localStorage.getItem('azkar_muted') === '1');
  const ctxRef = useRef(null);
  const mutedRef = useRef(muted);
  mutedRef.current = muted;

  const ctx = () => {
    if (!ctxRef.current) {
      try { ctxRef.current = new (window.AudioContext || window.webkitAudioContext)(); } catch (e) {}
    }
    return ctxRef.current;
  };
  const tone = (f, at, dur, type = 'sine', g = 0.06) => {
    if (mutedRef.current) return;
    const c = ctx();
    if (!c) return;
    try {
      const o = c.createOscillator();
      const gn = c.createGain();
      o.type = type;
      o.frequency.value = f;
      const t0 = c.currentTime + at;
      gn.gain.setValueAtTime(0.0001, t0);
      gn.gain.exponentialRampToValueAtTime(g, t0 + 0.012);
      gn.gain.exponentialRampToValueAtTime(0.0001, t0 + dur);
      o.connect(gn);
      gn.connect(c.destination);
      o.start(t0);
      o.stop(t0 + dur + 0.06);
    } catch (e) {}
  };

  return {
    muted,
    toggle: () => setMuted((m) => { localStorage.setItem('azkar_muted', m ? '0' : '1'); return !m; }),
    tap: () => tone(620, 0, 0.05, 'sine', 0.045),
    done: () => { tone(523, 0, 0.1, 'sine', 0.06); tone(784, 0.09, 0.16, 'sine', 0.06); },
    fanfare: () => {
      tone(523, 0, 0.14); tone(659, 0.11, 0.14); tone(784, 0.22, 0.16); tone(1047, 0.34, 0.4, 'sine', 0.07);
    },
  };
}

const getRiyadhHour = () => {
  try {
    const h = new Date().toLocaleString('en-US', { timeZone: 'Asia/Riyadh', hour: 'numeric', hour12: false });
    const n = parseInt(h, 10);
    return isNaN(n) ? new Date().getHours() : n;
  } catch (e) { return new Date().getHours(); }
};
const initialMode = () => {
  const saved = localStorage.getItem('azkar_last_mode');
  if (saved === 'morning' || saved === 'evening') return saved;
  const rh = getRiyadhHour();
  // بعد العصر (15) حتى الفجر (5) => مساء، غير ذلك صباح
  if (rh >= 15 || rh < 5) return 'evening';
  return 'morning';
};

const setupAzkarNotifications = async () => {
  const isNative = (() => { try { return window.Capacitor && window.Capacitor.isNativePlatform && window.Capacitor.isNativePlatform(); } catch (e) { return false; } })();
  const getPrayerTimes = async () => {
    // حاول الحصول على الموقع، وإلا استخدم الرياض كافتراضي
    let lat = 24.7136, lng = 46.6753; // الرياض
    try {
      const pos = await new Promise((res, rej) => {
        if (!navigator.geolocation) return rej('no geo');
        navigator.geolocation.getCurrentPosition(res, rej, { timeout: 8000, maximumAge: 600000 });
      });
      lat = pos.coords.latitude; lng = pos.coords.longitude;
    } catch (e) {}
    try {
      const { Coordinates, CalculationMethod, PrayerTimes } = await import('adhan');
      const coords = new Coordinates(lat, lng);
      const params = CalculationMethod.MuslimWorldLeague();
      params.madhab = 'Shafi';
      const date = new Date();
      const pt = new PrayerTimes(coords, date, params);
      return { fajr: pt.fajr, asr: pt.asr };
    } catch (e) {
      // fallback ثابت بتوقيت السعودية
      const now = new Date();
      const fajr = new Date(now); fajr.setHours(5, 5, 0, 0);
      const asr = new Date(now); asr.setHours(15, 30, 0, 0);
      return { fajr, asr };
    }
  };
  if (isNative) {
    try {
      const { LocalNotifications } = await import('@capacitor/local-notifications');
      const perm = await LocalNotifications.requestPermissions();
      if (perm.display !== 'granted') return;
      await LocalNotifications.cancel({ notifications: [{ id: 1 }, { id: 2 }, { id: 3 }, { id: 4 }] }).catch(() => {});
      const { fajr, asr } = await getPrayerTimes();
      const addMin = (d, m) => new Date(d.getTime() + m*60000);
      const notifs = [];
      // 10 دقائق بعد الفجر
      let t1 = addMin(fajr, 10);
      if (t1 <= new Date()) t1.setDate(t1.getDate()+1);
      notifs.push({ id: 1, title: 'حِصن - أذكار الصباح', body: 'أصبحنا وأصبح الملك لله - لا تنس أذكار الصباح', schedule: { at: t1.toISOString(), repeats: true, every: 'day' }, smallIcon: 'ic_stat_hissn', largeIcon: 'ic_launcher', iconColor: '#C79A4F' });
      // 4 ساعات بعد التذكير الأول صباحا
      let t2 = addMin(t1, 240);
      notifs.push({ id: 2, title: 'حِصن - تذكير الصباح', body: 'مضت 4 ساعات - أكمل أذكار الصباح', schedule: { at: t2.toISOString(), repeats: true, every: 'day' }, smallIcon: 'ic_stat_hissn', iconColor: '#C79A4F' });
      // 10 دقائق بعد العصر
      let t3 = addMin(asr, 10);
      if (t3 <= new Date()) t3.setDate(t3.getDate()+1);
      notifs.push({ id: 3, title: 'حِصن - أذكار المساء', body: 'أمسينا وأمسى الملك لله - حان وقت أذكار المساء', schedule: { at: t3.toISOString(), repeats: true, every: 'day' }, smallIcon: 'ic_stat_hissn', largeIcon: 'ic_launcher', iconColor: '#E3BC7A' });
      let t4 = addMin(t3, 120);
      notifs.push({ id: 4, title: 'حِصن - تذكير المساء', body: 'مضت ساعتان - أكمل أذكار المساء', schedule: { at: t4.toISOString(), repeats: true, every: 'day' }, smallIcon: 'ic_stat_hissn', iconColor: '#E3BC7A' });
      await LocalNotifications.schedule({ notifications: notifs });
    } catch (e) {}
    return;
  }
  if ('Notification' in window && Notification.permission !== 'denied') {
    try { if (Notification.permission === 'default') await Notification.requestPermission(); } catch (e) {}
  }
};

const shareCompletionImage = async (isMorning, streak) => {
  const label = isMorning ? 'أذكار الصباح' : 'أذكار المساء';
  const hijri = (() => { try { return new Intl.DateTimeFormat('ar-SA-u-ca-islamic-umalqura', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' }).format(new Date()); } catch (e) { return new Date().toLocaleDateString('ar-EG'); } })();
  const W = 1080, H = 1350;
  const canvas = document.createElement('canvas');
  canvas.width = W; canvas.height = H;
  const ctx = canvas.getContext('2d');
  // خلفية متدرجة كالتطبيق
  const grad = ctx.createLinearGradient(0, 0, 0, H);
  if (isMorning) { grad.addColorStop(0, '#F6F1E4'); grad.addColorStop(0.5, '#FDF9EE'); grad.addColorStop(1, '#F7EDD8'); }
  else { grad.addColorStop(0, '#141A33'); grad.addColorStop(0.5, '#1E2142'); grad.addColorStop(1, '#2C2650'); }
  ctx.fillStyle = grad; ctx.fillRect(0, 0, W, H);
  // توهج علوي
  const glow = ctx.createRadialGradient(W/2, 260, 40, W/2, 260, 420);
  glow.addColorStop(0, isMorning ? 'rgba(247,201,102,0.35)' : 'rgba(227,188,122,0.22)');
  glow.addColorStop(1, 'rgba(227,188,122,0)');
  ctx.fillStyle = glow; ctx.fillRect(0, 0, W, 700);
  const ink = isMorning ? '#433A2C' : '#F1EEE5';
  const sub = isMorning ? '#8C8270' : '#9FA6C8';
  const accent = isMorning ? '#C79A4F' : '#E3BC7A';
  // القوس مع الشمس/القمر
  ctx.strokeStyle = isMorning ? 'rgba(199,154,79,0.35)' : 'rgba(227,188,122,0.30)';
  ctx.lineWidth = 4; ctx.setLineDash([12, 16]);
  ctx.beginPath(); ctx.moveTo(W/2 - 380, 430); ctx.quadraticCurveTo(W/2, 120, W/2 + 380, 430); ctx.stroke();
  ctx.setLineDash([]);
  const mx = W/2, my = 275;
  const mg = ctx.createRadialGradient(mx - 8, my - 10, 4, mx, my, 46);
  mg.addColorStop(0, isMorning ? '#FFF3D0' : '#FBF3DD');
  mg.addColorStop(1, isMorning ? '#F5C96E' : '#EBD09A');
  ctx.beginPath(); ctx.arc(mx, my, 44, 0, Math.PI*2); ctx.fillStyle = mg; ctx.shadowColor = accent; ctx.shadowBlur = 40; ctx.fill(); ctx.shadowBlur = 0;
  if (!isMorning) { ctx.beginPath(); ctx.arc(mx + 18, my - 12, 36, 0, Math.PI*2); ctx.fillStyle = '#1E2142'; ctx.fill(); }
  ctx.font = '900 34px Cairo, sans-serif'; ctx.textAlign = 'center'; ctx.fillStyle = sub;
  ctx.fillText(isMorning ? '☀️' : '🌙', mx + 150, 470);
  // العنوان الرئيسي
  ctx.fillStyle = accent; ctx.font = '800 40px Cairo, sans-serif';
  ctx.fillText('تقبّل الله منك', W/2, 560);
  ctx.fillStyle = ink; ctx.font = '900 72px Cairo, sans-serif';
  ctx.fillText(`أتممتَ ${label}`, W/2, 650);
  // التاريخ الهجري
  ctx.fillStyle = sub; ctx.font = '600 34px Cairo, sans-serif';
  ctx.fillText(hijri, W/2, 720);
  // بطاقة الأيام المتتالية
  const bw = 560, bh = 120, bx = (W - bw)/2, by = 800, br = 60;
  ctx.fillStyle = isMorning ? 'rgba(255,255,255,0.85)' : 'rgba(255,255,255,0.07)';
  ctx.beginPath(); ctx.roundRect(bx, by, bw, bh, br); ctx.fill();
  ctx.strokeStyle = isMorning ? 'rgba(199,154,79,0.45)' : 'rgba(227,188,122,0.4)'; ctx.lineWidth = 3;
  ctx.beginPath(); ctx.roundRect(bx, by, bw, bh, br); ctx.stroke();
  ctx.font = '900 56px Cairo, sans-serif'; ctx.fillStyle = '#E2703A';
  ctx.fillText('🔥', bx + 110, by + 78);
  ctx.font = '900 48px Cairo, sans-serif'; ctx.fillStyle = ink; ctx.textAlign = 'right';
  ctx.fillText(`${streak}`, bx + bw - 200, by + 72);
  ctx.font = '700 30px Cairo, sans-serif'; ctx.fillStyle = sub;
  ctx.fillText('يوم متتالٍ', bx + bw - 200, by + 108);
  ctx.textAlign = 'center';
  // شعار حصن
  ctx.fillStyle = accent; ctx.font = '900 96px Cairo, sans-serif';
  ctx.shadowColor = accent; ctx.shadowBlur = 26;
  ctx.fillText('حِصن', W/2, 1090);
  ctx.shadowBlur = 0;
  ctx.fillStyle = sub; ctx.font = '600 30px Cairo, sans-serif';
  ctx.fillText('حصن المسلم اليومي', W/2, 1140);
  // خط ذهبي
  ctx.fillStyle = accent; ctx.globalAlpha = 0.6;
  ctx.beginPath(); ctx.roundRect(W/2 - 90, 1180, 180, 5, 3); ctx.fill();
  ctx.globalAlpha = 1;
  return new Promise((resolve) => {
    canvas.toBlob(async (blob) => {
      if (!blob) return resolve(false);
      const file = new File([blob], 'hissn-congrats.png', { type: 'image/png' });
      try {
        if (navigator.canShare && navigator.canShare({ files: [file] })) { await navigator.share({ files: [file], title: 'حِصن', text: `أتممت ${label} - ${hijri}` }); return resolve(true); }
      } catch (e) {}
      try {
        const { Share } = await import('@capacitor/share');
        const reader = new FileReader();
        reader.onloadend = async () => {
          const base64 = reader.result;
          try { await Share.share({ title: 'حِصن', text: `أتممت ${label}`, url: base64 }); } catch (e) {}
          resolve(true);
        };
        reader.readAsDataURL(blob);
        return;
      } catch (e) {}
      const url = URL.createObjectURL(blob);
      const a = document.createElement('a'); a.href = url; a.download = 'hissn-congrats.png'; a.click(); URL.revokeObjectURL(url);
      resolve(true);
    }, 'image/png', 0.92);
  });
};

/* ------------------------------------------------------------------ */
/* Component                                                           */
/* ------------------------------------------------------------------ */

export default function AzkarApp() {
  const [mode, setMode] = useState(initialMode);
  const list = mode === 'morning' ? MORNING : EVENING;
  const isMorning = mode === 'morning';

  const [remaining, setRemaining] = useState(() => loadState(mode).r);
  const [openDone, setOpenDone] = useState(() => loadState(mode).o);
  const [index, setIndex] = useState(() => loadState(mode).i);
  const [openCount, setOpenCount] = useState(0);
  const [justTapped, setJustTapped] = useState(false);
  const [hukmOpen, setHukmOpen] = useState(false);
  const [toast, setToast] = useState(null);
  const toastTimer = useRef(null);

  const [streak, setStreak] = useState(() => {
    try {
      const s = JSON.parse(localStorage.getItem('azkar_streak') || '{}');
      return s.last === dateStr() ? (s.n || 0) : 0;
    } catch (e) { return 0; }
  });
  const [doneMorning, setDoneMorning] = useState(() => readDone('morning'));
  const [doneEvening, setDoneEvening] = useState(() => readDone('evening'));
  const [celebrateTick, setCelebrateTick] = useState(0);
  const [remindOpen, setRemindOpen] = useState(false);
  const [weekOpen, setWeekOpen] = useState(false);
  const celebratedRef = useRef(false);

  const sound = useSound();

  const showToast = (msg, icon = '') => {
    setToast({ id: Date.now(), msg, icon });
    clearTimeout(toastTimer.current);
    toastTimer.current = setTimeout(() => setToast(null), 3000);
  };

  const updateStreak = () => {
    let stored = { n: 0, last: '' };
    try { stored = JSON.parse(localStorage.getItem('azkar_streak') || '{}'); } catch (e) {}
    const today = dateStr();
    const yest = dateStr(-1);
    if (stored.last === today) { setStreak(stored.n || 0); return stored.n || 0; }
    const n = stored.last === yest ? (stored.n || 0) + 1 : 1;
    try { localStorage.setItem('azkar_streak', JSON.stringify({ n, last: today })); } catch (e) {}
    setStreak(n);
    return n;
  };

  const markDone = () => {
    try {
      const d = JSON.parse(localStorage.getItem('azkar_done') || '{}');
      d[isMorning ? 'morning' : 'evening'] = dateStr();
      localStorage.setItem('azkar_done', JSON.stringify(d));
      saveHistory(dateStr(), isMorning ? 'morning' : 'evening');
    } catch (e) {}
    if (isMorning) setDoneMorning(true); else setDoneEvening(true);
  };

  /* load progress on mode change */
  useEffect(() => {
    const s = loadState(mode);
    setRemaining(s.r);
    setOpenDone(s.o);
    setIndex(s.i);
    setOpenCount(0);
    try { localStorage.setItem('azkar_last_mode', mode); } catch (e) {}
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [mode]);

  /* persist progress */
  useEffect(() => {
    try {
      localStorage.setItem(`azkar_p_${dateStr()}_${mode}`, JSON.stringify({ r: remaining, o: openDone, i: index }));
    } catch (e) {}
  }, [remaining, openDone, index, mode]);

  useEffect(() => { setupAzkarNotifications(); }, []);

  useEffect(() => { setOpenCount(0); }, [index]);

  /* computed */
  const total = list.length;
  const isDoneAt = (i) => (list[i].open ? !!openDone[i] : remaining[i] === 0);
  const doneCount = list.reduce((acc, _, i) => acc + (isDoneAt(i) ? 1 : 0), 0);
  const progress = total ? doneCount / total : 0;
  const allDone = doneCount === total;
  const safeIndex = Math.min(index, total - 1);
  const current = list[safeIndex];
  const curIsOpen = !!current.open;
  const curRemaining = curIsOpen ? null : remaining[safeIndex];
  const curDone = isDoneAt(safeIndex);
  const todayDone = isMorning ? doneMorning : doneEvening;

  /* celebration on completing all */
  useEffect(() => {
    if (allDone) {
      if (!celebratedRef.current && !todayDone) {
        celebratedRef.current = true;
        sound.fanfare();
        setCelebrateTick((t) => t + 1);
        const n = updateStreak();
        markDone();
        const label = isMorning ? 'الصباح' : 'المساء';
        setTimeout(() => showToast(`أتممت أذكار ${label}!${n > 0 ? ` سلسلة ${toAr(n)} أيام 🔥` : ''}`, '🎉'), 350);
        if (n === 3) setTimeout(() => showToast('إنجاز جديد: سلسلة ٣ أيام!', '🏅'), 2100);
        if (n === 7) setTimeout(() => showToast('إنجاز جديد: سلسلة ٧ أيام!', '🌟'), 2100);
        if (n === 30) setTimeout(() => showToast('إنجاز جديد: سلسلة شهر كامل!', '👑'), 2100);
      }
    } else {
      celebratedRef.current = false;
    }
    // eslint-disable-next-line react-hooks/exhaustive-deps
  }, [allDone]);

  /* actions */
  const tap = () => {
    if (allDone) return;
    if (curIsOpen) {
      setOpenCount((c) => c + 1);
      sound.tap();
      vibrate(10);
      return;
    }
    if (curRemaining === 0) return;
    setJustTapped(true);
    setTimeout(() => setJustTapped(false), 140);
    sound.tap();
    vibrate(10);
    const nextRemaining = [...remaining];
    nextRemaining[safeIndex] = Math.max(0, curRemaining - 1);
    setRemaining(nextRemaining);
    if (nextRemaining[safeIndex] === 0) {
      sound.done();
      vibrate(25);
      if (safeIndex < total - 1) setTimeout(() => setIndex((i) => Math.min(total - 1, i + 1)), 650);
    }
  };

  const finishOpen = () => {
    if (curDone) return;
    const no = [...openDone];
    no[safeIndex] = true;
    setOpenDone(no);
    sound.done();
    vibrate(25);
    if (safeIndex < total - 1) setTimeout(() => setIndex((i) => Math.min(total - 1, i + 1)), 650);
  };

  const goNext = () => setIndex((i) => Math.min(total - 1, i + 1));
  const goPrev = () => setIndex((i) => Math.max(0, i - 1));
  const resetAll = () => {
    setRemaining(list.map((d) => (d.open ? null : d.n)));
    setOpenDone(list.map(() => false));
    setIndex(0);
    setOpenCount(0);
    showToast('أُعيدت الأذكار من البداية', '🔄');
  };
  const switchMode = (m) => {
    if (m === mode) return;
    setHukmOpen(false);
    setMode(m);
  };

  /* palette — ألوان هادئة */
  const palette = isMorning
    ? {
        bg: 'linear-gradient(180deg, #F6F1E4 0%, #FDF9EE 50%, #F7EDD8 100%)',
        ink: '#433A2C',
        sub: '#8C8270',
        accent: '#C79A4F',
        accentStrong: '#A87E35',
        accentSoft: 'rgba(199,154,79,0.14)',
        card: 'rgba(255,255,255,0.82)',
        cardBorder: 'rgba(199,154,79,0.32)',
        chip: 'rgba(199,154,79,0.10)',
        onAccent: '#4A3A1C',
        succ: '#6F9E7F',
        teal: '#4F8D93',
        warn: '#C97F4F',
      }
    : {
        bg: 'linear-gradient(180deg, #141A33 0%, #1E2142 50%, #2C2650 100%)',
        ink: '#F1EEE5',
        sub: '#9FA6C8',
        accent: '#E3BC7A',
        accentStrong: '#C89A52',
        accentSoft: 'rgba(227,188,122,0.14)',
        card: 'rgba(255,255,255,0.055)',
        cardBorder: 'rgba(227,188,122,0.26)',
        chip: 'rgba(227,188,122,0.10)',
        onAccent: '#3A2C14',
        succ: '#7FB69B',
        teal: '#6FB0B8',
        warn: '#D9A06A',
      };

  const { x: arcX, y: arcY } = arcPoint(progress);
  const hour = new Date().getHours();
  const greeting = hour < 5 ? 'وَقُرْآنَ الْفَجْرِ، حيّاك الله' : hour < 12 ? 'صباحك نور وبركة' : hour < 17 ? 'نهارك مبارك' : 'مساءك خير ورحمة';
  const hijriLabel = (() => {
    try {
      const fmt = new Intl.DateTimeFormat('ar-SA-u-ca-islamic-umalqura', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' });
      const s = fmt.format(new Date());
      if (s.includes('محرم') || s.includes('صفر') || s.includes('ربيع') || s.includes('جمادى') || s.includes('رجب') || s.includes('شعبان') || s.includes('رمضان') || s.includes('شوال') || s.includes('ذو')) return s;
      throw new Error('not hijri');
    } catch (e) {
      try { return new Intl.DateTimeFormat('ar-SA-u-ca-islamic', { weekday: 'long', day: 'numeric', month: 'long', year: 'numeric' }).format(new Date()); } catch (e2) { return new Date().toLocaleDateString('ar-EG', { weekday: 'long', day: 'numeric', month: 'long' }); }
    }
  })();
  const dateLabel = hijriLabel;

  /* confetti particles */
  const particles = useMemo(
    () =>
      Array.from({ length: 70 }, () => {
        const colors = [palette.accent, palette.succ, '#E8C07A', '#C98A9E', '#8FB9C9', '#F2D9A0'];
        return {
          left: Math.random() * 100,
          delay: Math.random() * 0.9,
          dur: 2.4 + Math.random() * 1.8,
          size: 6 + Math.random() * 8,
          w: 0.4 + Math.random() * 0.6,
          color: colors[Math.floor(Math.random() * colors.length)],
          tilt: Math.random() * 360,
        };
      }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [celebrateTick]
  );

  const RING_R = 37;
  const RING_C = 2 * Math.PI * RING_R;

  return (
    <div
      dir="rtl"
      style={{
        height: '100dvh',
        minHeight: '100dvh',
        maxHeight: '100dvh',
        background: palette.bg,
        fontFamily: "'Cairo', sans-serif",
        color: palette.ink,
        transition: 'background 700ms ease, color 700ms ease',
        display: 'flex',
        justifyContent: 'center',
        position: 'relative',
        overflow: 'auto',
      }}
    >
      <style>{`
        @import url('https://fonts.googleapis.com/css2?family=Amiri:ital,wght@0,400;0,700;1,400&family=Cairo:wght@400;500;600;700;800;900&display=swap');
        * { box-sizing: border-box; -webkit-tap-highlight-color: transparent; margin: 0; padding: 0; }
        html, body, #root { margin: 0; padding: 0; min-height: 100%; }
        body { background: ${palette.bg}; overscroll-behavior: none; }
        .dhikr-text { font-family: 'Cairo', sans-serif; font-weight: 600; }
        .quran-intro {
          font-family: 'Cairo', sans-serif;
          font-weight: 800;
          line-height: 2.1;
          background: linear-gradient(180deg, #ffe3a1 0%, #eab94b 48%, #9c7010 100%);
          -webkit-background-clip: text;
          background-clip: text;
          color: transparent;
          filter: drop-shadow(0 1px 1px rgba(0,0,0,.22));
        }
        .remind-title {
          background: linear-gradient(90deg, ${palette.accent}, #ffd76e, ${palette.accent});
          background-size: 200% auto;
          -webkit-background-clip: text;
          background-clip: text;
          color: transparent;
          animation: shimmerText 3s linear infinite;
        }
        @keyframes shimmerText { 0% { background-position: 200% center; } 100% { background-position: -200% center; } }
        @keyframes floatY { 0%, 100% { transform: translateY(0); } 50% { transform: translateY(-4px); } }
        @keyframes riseIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: none; } }
        .rise { animation: riseIn 460ms cubic-bezier(.2,.9,.3,1.05) both; }
        @keyframes plantSway { 0%,100% { transform: rotate(-1.2deg); } 50% { transform: rotate(1.2deg); } }
        @keyframes arcDot { 0%,100% { opacity:.35 } 50% { opacity:1 } }
        .glow-particle {
          position: absolute; border-radius: 50%; filter: blur(40px); pointer-events: none;
          animation: drift 18s ease-in-out infinite;
        }
        @keyframes drift {
          0%, 100% { transform: translate(0,0) scale(1); }
          50% { transform: translate(14px,-18px) scale(1.1); }
        }
        @keyframes popIn { from { opacity: 0; transform: translateY(10px) scale(.985); } to { opacity: 1; transform: translateY(0) scale(1); } }
        .card-anim { animation: popIn 340ms cubic-bezier(.2,.9,.3,1.05); }
        @keyframes ripple { from { transform: scale(.62); opacity: .8; } to { transform: scale(1.28); opacity: 0; } }
        @keyframes floatBob { 0%, 100% { transform: translate(-50%,-50%) translateY(0); } 50% { transform: translate(-50%,-50%) translateY(-5px); } }
        @keyframes spinSlow { to { transform: rotate(360deg); } }
        @keyframes twinkle { 0%, 100% { opacity: .25; transform: scale(.8); } 50% { opacity: 1; transform: scale(1.15); } }
        @keyframes haloPulse { 0%, 100% { opacity: .4; transform: scale(1); } 50% { opacity: .85; transform: scale(1.15); } }
        @keyframes confettiFall {
          0% { transform: translateY(-10vh) rotate(0deg); opacity: 1; }
          100% { transform: translateY(108vh) rotate(720deg); opacity: .65; }
        }
        @keyframes glowPulse {
          0%, 100% { box-shadow: 0 0 0 0 ${palette.accent}66; }
          50% { box-shadow: 0 0 0 10px ${palette.accent}00; }
        }
        @keyframes bounceIn { 0% { transform: scale(.4); opacity: 0; } 60% { transform: scale(1.12); } 100% { transform: scale(1); opacity: 1; } }
        @keyframes slideUp { from { transform: translateY(70px); opacity: 0; } to { transform: translateY(0); opacity: 1; } }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        @keyframes toastIn { from { opacity: 0; transform: translate(-50%, 14px) scale(.92); } to { opacity: 1; transform: translate(-50%, 0) scale(1); } }
        .count-tap { transition: transform 130ms ease; }
        .count-tap:active { transform: scale(.9); }
        .seg-cur { animation: segPulse 1.6s ease-in-out infinite; }
        @keyframes segPulse { 0%, 100% { opacity: .5; } 50% { opacity: 1; } }
        @media (prefers-reduced-motion: reduce) {
          .glow-particle, .seg-cur, .count-tap { animation: none; transition: none; }
          .card-anim { animation: none; }
        }
        ::selection { background: ${palette.accentSoft}; }
      `}</style>

      {/* ambient light */}
      <div className="glow-particle" style={{ width: 230, height: 230, top: -70, right: -50, background: palette.accent, opacity: isMorning ? 0.16 : 0.10 }} />
      <div className="glow-particle" style={{ width: 190, height: 190, bottom: 40, left: -60, background: isMorning ? '#8FB9C9' : '#6C5CA8', opacity: 0.14, animationDelay: '4s' }} />

      <div style={{ width: '100%', maxWidth: 480, padding: '16px 14px 24px', position: 'relative', zIndex: 1 }}>

        {/* header */}
        <div className="rise" style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', marginBottom: 8 }}>
          <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
            <button onClick={() => setWeekOpen(true)} style={{ display: 'flex', alignItems: 'center', gap: 5, background: palette.chip, border: `1px solid ${palette.cardBorder}`, padding: '6px 13px', borderRadius: 999, fontWeight: 800, fontSize: 13, cursor: 'pointer', fontFamily: "'Cairo', sans-serif", color: palette.ink }}>
              <Flame size={15} color="#E2703A" fill="#E2703A" />
              {toAr(streak)} <span style={{ fontSize: 10.5, fontWeight: 600, color: palette.sub }}>يوم متتالٍ</span>
            </button>
          </div>
          <div style={{ display: 'flex', gap: 8, alignItems: 'center' }}>
            <button
              onClick={sound.toggle}
              aria-label="كتم الصوت"
              title={sound.muted ? 'تشغيل الصوت' : 'كتم الصوت'}
              style={{ width: 34, height: 34, borderRadius: '50%', background: palette.chip, border: `1px solid ${palette.cardBorder}`, display: 'flex', alignItems: 'center', justifyContent: 'center', cursor: 'pointer', color: palette.sub }}
            >
              {sound.muted ? <VolumeX size={15} /> : <Volume2 size={15} />}
            </button>
          </div>
        </div>

        {/* title */}
        <div className="rise" style={{ textAlign: 'center', marginBottom: 6, animationDelay: '60ms' }}>
          <div style={{ fontSize: 12, color: palette.sub, fontWeight: 600, marginBottom: 4 }}>{greeting} — {dateLabel}</div>
          <div style={{ fontSize: 24, fontWeight: 900, marginBottom: 2 }}>
            أذكار {isMorning ? 'الصباح' : 'المساء'}
          </div>
        </div>

        {/* time hint */}
        {(isMorning && hour >= 17) || (!isMorning && hour >= 5 && hour < 12) ? (
          <button
            onClick={() => switchMode(isMorning ? 'evening' : 'morning')}
            style={{
              display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 6, margin: '0 auto 12px',
              background: palette.accentSoft, border: `1px solid ${palette.cardBorder}`, color: palette.accentStrong,
              borderRadius: 999, padding: '7px 16px', fontSize: 12, fontWeight: 800, cursor: 'pointer',
              fontFamily: "'Cairo', sans-serif",
            }}
          >
            {isMorning ? <>حان وقت أذكار المساء 🌙 اضغط للانتقال</> : <>وقت أذكار الصباح ☀️ اضغط للانتقال</>}
          </button>
        ) : null}
{/* شريط الشمس والقمر البسيط */}
        <div className="rise" style={{ animationDelay: '90ms', position: 'relative', height: 46, margin: '0 0 2px', flexShrink: 0 }}>
          <svg viewBox="0 0 100 40" preserveAspectRatio="none" style={{ width: '100%', height: '100%', overflow: 'visible' }}>
            <path d="M 6 34 Q 50 2 94 34" fill="none" stroke={palette.cardBorder} strokeWidth="1" strokeDasharray="2 3" strokeLinecap="round" />
          </svg>
          {[0.25, 0.5, 0.75].map((tt) => {
            const pp = arcPoint(tt);
            const filled = progress >= tt;
            return (
              <span key={tt} style={{ position: 'absolute', left: `${pp.x}%`, top: `${(pp.y / 40) * 100}%`, width: 5, height: 5, borderRadius: '50%', transform: 'translate(-50%,-50%)', background: filled ? palette.accent : palette.chip, boxShadow: filled ? `0 0 8px ${palette.accent}` : 'none', transition: 'all 400ms ease' }} />
            );
          })}
          <div style={{ position: 'absolute', left: `${arcX}%`, top: `${(arcY / 40) * 100}%`, transform: 'translate(-50%,-50%)', transition: 'left 600ms cubic-bezier(.2,.9,.3,1.1), top 600ms cubic-bezier(.2,.9,.3,1.1)' }}>
            <div style={{ position: 'relative', width: 22, height: 22 }}>
              <div style={{ position: 'absolute', inset: -6, borderRadius: '50%', background: isMorning ? 'radial-gradient(circle, rgba(247,201,102,.4), transparent 70%)' : 'radial-gradient(circle, rgba(227,188,122,.28), transparent 70%)', animation: 'haloPulse 3.2s ease-in-out infinite' }} />
              <div style={{ width: 22, height: 22, borderRadius: '50%', background: isMorning ? 'radial-gradient(circle at 35% 30%, #FFF3D0, #F5C96E)' : 'radial-gradient(circle at 35% 30%, #FBF3DD, #EBD09A)', boxShadow: `0 0 14px ${palette.accent}77`, animation: 'floatY 3s ease-in-out infinite' }}>
                {!isMorning && (
                  <span style={{ WebkitMaskImage: 'radial-gradient(circle 5px at 68% 30%, transparent 98%, #000 99%)', maskImage: 'radial-gradient(circle 5px at 68% 30%, transparent 98%, #000 99%)', display: 'block', width: '100%', height: '100%', borderRadius: '50%' }} />
                )}
              </div>
              {!isMorning && <span style={{ position: 'absolute', top: -5, left: -7, fontSize: 8, color: palette.accent, animation: 'twinkle 2.6s ease-in-out infinite' }}>✦</span>}
            </div>
          </div>
        </div>

                {/* segmented control */}
        <div className="rise" style={{ display: 'flex', background: palette.chip, borderRadius: 999, padding: 4, gap: 4, marginBottom: 8, border: `1px solid ${palette.cardBorder}`, animationDelay: '120ms', flexShrink: 0 }}>
          {[
            { key: 'morning', label: 'أذكار الصباح', Icon: Sun },
            { key: 'evening', label: 'أذكار المساء', Icon: Moon },
          ].map(({ key, label, Icon }) => {
            const active = mode === key;
            const done = key === 'morning' ? doneMorning : doneEvening;
            return (
              <button
                key={key}
                onClick={() => switchMode(key)}
                style={{
                  flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 7,
                  padding: '8px 0', borderRadius: 999, border: 'none', cursor: 'pointer',
                  fontFamily: "'Cairo', sans-serif", fontWeight: 800, fontSize: 13.5,
                  color: active ? palette.onAccent : palette.sub,
                  background: active ? palette.accent : 'transparent',
                  transition: 'all 250ms ease',
                }}
              >
                <Icon size={15} />
                {label}
                {done && (
                  <span style={{
                    width: 16, height: 16, borderRadius: '50%',
                    background: active ? 'rgba(255,255,255,.9)' : '#4FA97A',
                    display: 'inline-flex', alignItems: 'center', justifyContent: 'center',
                    fontSize: 9, color: active ? '#4F8D6F' : '#fff',
                  }}>
                    <Check size={9} strokeWidth={4} />
                  </span>
                )}
              </button>
            );
          })}
        </div>

        {/* progress segments */}
        <div className="rise" style={{ display: 'flex', gap: 3, marginBottom: 8, animationDelay: '160ms', flexShrink: 0 }}>
          {list.map((d, i) => {
            const st = isDoneAt(i) ? 'done' : i === safeIndex ? 'cur' : 'todo';
            return (
              <button
                key={i}
                title={d.s || d.t.slice(0, 40)}
                onClick={() => setIndex(i)}
                className={st === 'cur' ? 'seg-cur' : undefined}
                style={{
                  flex: 1, height: 7, borderRadius: 4, border: 'none', cursor: 'pointer',
                  background: st === 'done' ? palette.accent : st === 'cur' ? palette.accentSoft : palette.chip,
                  boxShadow: st === 'cur' ? `inset 0 0 0 1.5px ${palette.accent}` : 'none',
                  transition: 'all 250ms ease', opacity: st === 'todo' ? 0.75 : 1,
                }}
              />
            );
          })}
        </div>

        {allDone ? (
          /* completion state */
          <div
            className="card-anim"
            style={{
              background: palette.card, border: `1px solid ${palette.cardBorder}`, borderRadius: 26,
              padding: '20px 18px 16px', textAlign: 'center', backdropFilter: 'blur(10px)',
              boxShadow: isMorning ? '0 10px 34px rgba(180,140,80,0.14)' : '0 10px 34px rgba(0,0,0,0.3)',
              flex: 1, minHeight: 0, display: 'flex', flexDirection: 'column', justifyContent: 'center', overflow: 'auto',
            }}
          >
            <div style={{
              width: 76, height: 76, borderRadius: '50%', background: palette.accentSoft,
              display: 'flex', alignItems: 'center', justifyContent: 'center', margin: '0 auto 14px',
              animation: 'bounceIn 520ms cubic-bezier(.3,1.4,.5,1)',
              boxShadow: `0 0 0 8px ${palette.accentSoft}`,
            }}>
              <Trophy size={34} color={palette.accent} />
            </div>
            <div style={{ fontSize: 20, fontWeight: 900, marginBottom: 6 }}>
              ما شاء الله، أتممتَ أذكار {isMorning ? 'الصباح' : 'المساء'}!
            </div>
            <div style={{ fontSize: 13.5, color: palette.sub, lineHeight: 2, marginBottom: 18 }}>
              تقبّل الله منك، وجعلها في ميزان حسناتك.
              <br />
              اللهم اجعلنا من الذاكرين الله كثيرًا والذاكرات.
            </div>

            <div style={{ display: 'flex', justifyContent: 'center', gap: 9, marginBottom: 16, flexWrap: 'wrap' }}>
              <button onClick={() => setWeekOpen(true)} style={{ display: 'flex', alignItems: 'center', gap: 5, background: palette.chip, border: `1px solid ${palette.cardBorder}`, borderRadius: 999, padding: '7px 13px', fontSize: 12.5, fontWeight: 800, cursor: 'pointer', fontFamily: "'Cairo', sans-serif", color: palette.ink }}>
                <Flame size={14} color="#E2703A" fill="#E2703A" /> {toAr(streak)} يوم متتالٍ
              </button>
            </div>

            <div
              onClick={() => setRemindOpen(o => !o)}
              onMouseDown={(e) => (e.currentTarget.style.transform = 'scale(.97)')}
              onMouseUp={(e) => (e.currentTarget.style.transform = 'scale(1)')}
              style={{
                display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 13, marginBottom: 20,
                padding: '15px 16px', borderRadius: 18, cursor: 'pointer', textAlign: 'right',
                background: `linear-gradient(135deg, ${palette.accent}26, ${palette.chip})`,
                border: `1.5px solid ${palette.accent}77`,
                boxShadow: `0 6px 26px ${palette.accent}22`,
                animation: 'glowPulse 2.4s ease-in-out infinite',
                transition: 'transform 150ms ease',
              }}
            >
              <BookOpen
                size={24} color={palette.accent} fill={palette.accent}
                style={{ flexShrink: 0, animation: 'floatY 2.6s ease-in-out infinite' }}
              />
              <span style={{ flex: 1 }}>
                <span className="remind-title" style={{ display: 'block', fontSize: 15.5, fontWeight: 900, lineHeight: 1.9 }}>
                  لا تنسَ وردك اليومي: قراءة ما تيسر من القرآن الكريم
                </span>
                <span style={{ display: 'flex', alignItems: 'center', gap: 5, fontSize: 11.5, fontWeight: 700, color: palette.sub }}>
                  اضغط لعرض الآية
                  <ChevronDown size={13} style={{ transition: 'transform 250ms ease', transform: remindOpen ? 'rotate(180deg)' : 'none' }} />
                </span>
              </span>
            </div>
            {remindOpen && (
              <div className="quran-intro" style={{ fontSize: 19, textAlign: 'center', lineHeight: 2.3, marginBottom: 20, animation: 'popIn 260ms ease' }}>
                ﴿ فَٱقۡرَءُواْ مَا تَيَسَّرَ مِنَ ٱلۡقُرۡءَانِ ﴾
              </div>
            )}

            <button onClick={async () => { const ok = await shareCompletionImage(isMorning, streak); if (!ok) showToast('تم تجهيز الصورة - جرب مرة أخرى', '📤'); }} style={{ width: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 8, padding: '9px 0', borderRadius: 14, border: 'none', background: palette.succ, color: 'white', fontFamily: "'Cairo', sans-serif", fontWeight: 800, fontSize: 14, cursor: 'pointer', marginBottom: 10 }}>
              <Share2 size={16} /> شارك
            </button>
            <div style={{ display: 'flex', gap: 10 }}>
              <button
                onClick={() => switchMode(isMorning ? 'evening' : 'morning')}
                style={{
                  flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 7,
                  padding: '9px 0', borderRadius: 14, border: 'none', background: palette.accent,
                  color: palette.onAccent, fontFamily: "'Cairo', sans-serif", fontWeight: 800, fontSize: 14, cursor: 'pointer',
                }}
              >
                {isMorning ? <Moon size={16} /> : <Sun size={16} />} أذكار {isMorning ? 'المساء' : 'الصباح'}
              </button>
              <button
                onClick={resetAll}
                style={{
                  flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 7,
                  padding: '9px 0', borderRadius: 14, border: `1px solid ${palette.cardBorder}`,
                  background: 'transparent', color: palette.ink, fontFamily: "'Cairo', sans-serif",
                  fontWeight: 700, fontSize: 14, cursor: 'pointer',
                }}
              >
                <RotateCcw size={15} /> إعادة
              </button>
            </div>
          </div>
        ) : (
          <>
            {/* progress label */}
            <div className="rise" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', fontSize: 12, color: palette.sub, marginBottom: 4, padding: '0 2px', fontWeight: 700, animationDelay: '200ms', flexShrink: 0 }}>
              <span>الذكر {toAr(safeIndex + 1)} من {toAr(total)}</span>
              <span>أُنجز {toAr(doneCount)} من {toAr(total)}</span>
            </div>

            {/* dhikr card */}
            <div
              key={mode + safeIndex}
              className="card-anim"
              style={{
                background: palette.card,
                border: `1px solid ${curDone ? palette.accent : palette.cardBorder}`,
                borderRadius: 20,
                padding: '12px 14px',
                backdropFilter: 'blur(10px)',
                boxShadow: curDone
                  ? `0 0 0 1px ${palette.accent}55, 0 10px 30px ${palette.accent}22`
                  : isMorning ? '0 8px 30px rgba(180,140,80,0.10)' : '0 8px 30px rgba(0,0,0,0.28)',
                transition: 'border-color 300ms ease, box-shadow 300ms ease',
                flex: 1,
                minHeight: 0,
                display: 'flex',
                flexDirection: 'column',
                overflow: 'hidden',
              }}
            >
              {/* header row: subtitle + hukm chip */}
              <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between', gap: 8, marginBottom: 8, flexShrink: 0 }}>
                {current.s ? (
                  <div style={{ fontSize: 12.5, fontWeight: 800, color: palette.accent, background: palette.accentSoft, padding: '6px 13px', borderRadius: 999 }}>
                    {current.s}
                  </div>
                ) : <span />}
                {current.h ? (
                  <button
                    onClick={() => setHukmOpen(true)}
                    style={{
                      display: 'flex', alignItems: 'center', gap: 5, background: palette.chip,
                      border: `1px solid ${palette.cardBorder}`, color: palette.sub, fontSize: 11,
                      fontWeight: 800, padding: '6px 11px', borderRadius: 999, cursor: 'pointer',
                      fontFamily: "'Cairo', sans-serif", transition: 'transform 120ms ease',
                    }}
                    onMouseDown={(e) => (e.currentTarget.style.transform = 'scale(.94)')}
                    onMouseUp={(e) => (e.currentTarget.style.transform = 'scale(1)')}
                  >
                    <BadgeCheck size={14} color={palette.accent} /> صحة الذكر
                  </button>
                ) : null}
              </div>

              {current.intro && (
                <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 12, marginBottom: 14 }}>
                  <span style={{ width: 46, height: 1.5, background: `linear-gradient(90deg, transparent, ${palette.accent}AA)` }} />
                  <span className="quran-intro" style={{ fontSize: 23 }}>{current.intro}</span>
                  <span style={{ width: 46, height: 1.5, background: `linear-gradient(270deg, transparent, ${palette.accent}AA)` }} />
                </div>
              )}

              <div className="dhikr-text" style={{ fontSize: 'clamp(12.5px, 3.3vw, 16.5px)', lineHeight: 1.9, textAlign: 'center', fontWeight: 600, flex: 1, minHeight: 0, display: 'flex', alignItems: 'center', justifyContent: 'center', overflow: 'hidden', padding: '2px 0' }}>
                {current.t}
              </div>



              {/* counter */}
              <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 26, flexShrink: 0 }}>
              {curIsOpen ? (
                <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', marginTop: 4, gap: 8 }}>
                  <button
                    onClick={tap}
                    className="count-tap"
                    aria-label="عدّ التسبيح"
                    style={{
                      position: 'relative', width: 86, height: 86, borderRadius: '50%',
                      border: 'none', background: 'transparent', cursor: 'pointer',
                      fontFamily: "'Cairo', sans-serif",
                    }}
                  >
                    <svg width={86} height={86} style={{ position: 'absolute', inset: 0, transform: 'rotate(-90deg)' }}>
                      <circle cx={43} cy={43} r={RING_R} fill="none" stroke={palette.chip} strokeWidth={9} strokeDasharray="5 8" strokeLinecap="round" />
                      <circle cx={43} cy={43} r={RING_R} fill="none" stroke={palette.accent} strokeWidth={9} opacity={0.28} />
                    </svg>
                    <span style={{ position: 'relative', display: 'flex', flexDirection: 'column', alignItems: 'center', justifyContent: 'center', width: '100%', height: '100%' }}>
                      <span key={openCount} className="card-anim" style={{ fontSize: 28, fontWeight: 900, lineHeight: 1 }}>
                        {toAr(openCount)}
                      </span>
                      <span style={{ fontSize: 10, fontWeight: 600, color: palette.sub, marginTop: 3 }}>عداد مفتوح</span>
                    </span>
                  </button>
                  <button
                    onClick={finishOpen}
                    disabled={curDone}
                    style={{
                      display: 'flex', alignItems: 'center', gap: 8, border: 'none', borderRadius: 999,
                      padding: '10px 22px', fontFamily: "'Cairo', sans-serif", fontWeight: 800, fontSize: 14,
                      background: curDone ? palette.succ : palette.accent,
                      color: curDone ? '#fff' : palette.onAccent, cursor: curDone ? 'default' : 'pointer',
                      boxShadow: !curDone ? `0 6px 18px ${palette.accent}44` : 'none',
                      transition: 'all 200ms ease',
                    }}
                  >
                    <Check size={16} strokeWidth={3.5} />
                    {curDone ? 'تمّ — بارك الله فيك' : 'أتممت هذا الذكر'}
                  </button>
                </div>
              ) : (
                <div style={{ display: 'flex', justifyContent: 'center', marginTop: 4 }}>
                  <button
                    onClick={tap}
                    className="count-tap"
                    aria-label="عدّ التكرار"
                    style={{
                      position: 'relative', width: 86, height: 86, borderRadius: '50%',
                      border: 'none', background: 'transparent', cursor: curRemaining === 0 ? 'default' : 'pointer',
                      fontFamily: "'Cairo', sans-serif",
                    }}
                  >
                    <svg width={86} height={86} style={{ position: 'absolute', inset: 0, transform: 'rotate(-90deg)' }}>
                      <circle cx={43} cy={43} r={RING_R} fill="none" stroke={palette.chip} strokeWidth={9} />
                      <circle
                        cx={43} cy={43} r={RING_R} fill="none" stroke={palette.accent} strokeWidth={9}
                        strokeLinecap="round" strokeDasharray={RING_C} strokeDashoffset={RING_C * (curRemaining / current.n)}
                        style={{ transition: 'stroke-dashoffset 220ms ease' }}
                      />
                    </svg>
                    <span style={{
                      position: 'relative', display: 'flex', flexDirection: 'column', alignItems: 'center',
                      justifyContent: 'center', width: '100%', height: '100%',
                      background: curDone ? palette.accentSoft : 'transparent',
                      borderRadius: '50%',
                    }}>
                      <span key={curRemaining} className="card-anim" style={{ fontSize: 26, fontWeight: 900, lineHeight: 1, color: curDone ? palette.accent : palette.ink }}>
                        {curRemaining === 0 ? <Check size={30} strokeWidth={3.5} /> : toAr(curRemaining)}
                      </span>
                      <span style={{ fontSize: 10.5, fontWeight: 600, color: palette.sub, marginTop: 4 }}>
                        {curRemaining === 0 ? 'أُنجز الذكر' : 'تبقّى'}
                      </span>
                    </span>
                    {justTapped && (
                      <span style={{ position: 'absolute', inset: 0, borderRadius: '50%', border: `2px solid ${palette.accent}`, animation: 'ripple 320ms ease-out forwards', pointerEvents: 'none' }} />
                    )}
                  </button>
                </div>
              )}
              </div>
            </div>

            {/* nav */}
            <div className="rise" style={{ display: 'flex', gap: 10, marginTop: 8, animationDelay: '240ms', flexShrink: 0 }}>
              <button
                onClick={goPrev}
                disabled={safeIndex === 0}
                style={{
                  flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 6,
                  padding: '9px 0', borderRadius: 14, border: `1px solid ${palette.cardBorder}`,
                  background: 'transparent', color: safeIndex === 0 ? palette.sub : palette.ink,
                  opacity: safeIndex === 0 ? 0.5 : 1, fontFamily: "'Cairo', sans-serif", fontWeight: 700, fontSize: 14,
                  cursor: safeIndex === 0 ? 'default' : 'pointer',
                }}
              >
                <ChevronRight size={17} /> السابق
              </button>
              <button
                onClick={goNext}
                disabled={safeIndex === total - 1}
                style={{
                  flex: 1, display: 'flex', alignItems: 'center', justifyContent: 'center', gap: 6,
                  padding: '9px 0', borderRadius: 14, border: 'none',
                  background: safeIndex === total - 1 ? palette.chip : palette.accent,
                  color: safeIndex === total - 1 ? palette.sub : palette.onAccent,
                  fontFamily: "'Cairo', sans-serif", fontWeight: 800, fontSize: 14,
                  cursor: safeIndex === total - 1 ? 'default' : 'pointer',
                  boxShadow: curDone && safeIndex < total - 1 ? `0 6px 20px ${palette.accent}55` : 'none',
                  animation: curDone && safeIndex < total - 1 ? 'glowPulse 1.4s ease-in-out infinite' : 'none',
                }}
              >
                {curDone && safeIndex < total - 1 ? <Check size={15} strokeWidth={3.5} /> : <ChevronLeft size={17} />} التالي
              </button>
            </div>

            <div className="rise" style={{ textAlign: 'center', marginTop: 4, animationDelay: '280ms', flexShrink: 0 }}>
              <button
                onClick={resetAll}
                style={{
                  background: 'none', border: 'none', color: palette.sub, fontSize: 12.5,
                  fontFamily: "'Cairo', sans-serif", display: 'inline-flex', alignItems: 'center', gap: 5, cursor: 'pointer', fontWeight: 600,
                }}
              >
                <RotateCcw size={12} /> إعادة تعيين العدّاد
              </button>
            </div>
          </>
        )}
      </div>

      {/* confetti */}
      {celebrateTick > 0 && (
        <div style={{ position: 'fixed', inset: 0, pointerEvents: 'none', zIndex: 60 }}>
          {particles.map((p, i) => (
            <span
              key={i}
              style={{
                position: 'absolute', left: `${p.left}%`, top: -20, width: p.size, height: p.size * p.w,
                borderRadius: 3, background: p.color, transform: `rotate(${p.tilt}deg)`,
                animation: `confettiFall ${p.dur}s ${p.delay}s linear forwards`,
              }}
            />
          ))}
        </div>
      )}

      {/* hukm sheet */}
      {hukmOpen && current.h && (
        <div style={{ position: 'fixed', inset: 0, zIndex: 70, display: 'flex', alignItems: 'flex-end', justifyContent: 'center' }}>
          <div
            onClick={() => setHukmOpen(false)}
            style={{ position: 'absolute', inset: 0, background: 'rgba(15,18,30,.45)', animation: 'fadeIn 200ms ease', backdropFilter: 'blur(4px)' }}
          />
          <div style={{
            position: 'relative', width: '100%', maxWidth: 480, color: palette.ink,
            background: isMorning ? '#FFFBF1' : '#232A45',
            borderRadius: '28px 28px 0 0', padding: '10px 20px 28px', maxHeight: '84vh', overflowY: 'auto',
            animation: 'slideUp 340ms cubic-bezier(.2,.9,.3,1.1)',
            fontFamily: "'Cairo', sans-serif",
          }}>
            <div style={{ width: 42, height: 5, borderRadius: 99, background: isMorning ? 'rgba(0,0,0,.12)' : 'rgba(255,255,255,.15)', margin: '4px auto 14px' }} />

            <div style={{ display: 'flex', alignItems: 'center', gap: 12, marginBottom: 16 }}>
              <div style={{ width: 42, height: 42, borderRadius: '50%', background: palette.accentSoft, display: 'flex', alignItems: 'center', justifyContent: 'center', flexShrink: 0 }}>
                <BadgeCheck size={20} color={palette.accent} />
              </div>
              <div style={{ flex: 1 }}>
                <div style={{ fontWeight: 900, fontSize: 16 }}>صحة الذكر</div>
                <div style={{ fontSize: 11.5, color: palette.sub, fontWeight: 600 }}>{current.s} — فضل الذكر والحكم ودرجة الحديث</div>
              </div>
              <button
                onClick={() => setHukmOpen(false)}
                aria-label="إغلاق"
                style={{ width: 34, height: 34, borderRadius: '50%', background: palette.chip, border: 'none', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', color: palette.sub, flexShrink: 0 }}
              >
                <X size={16} />
              </button>
            </div>

            {current.v && (
              <div style={{ background: palette.chip, borderRadius: 16, padding: '13px 15px', marginBottom: 14 }}>
                <div style={{ fontSize: 11.5, fontWeight: 800, color: palette.accent, marginBottom: 6 }}>✨ فضل الذكر</div>
                <div style={{ fontSize: 13.5, lineHeight: 2, fontWeight: 600 }}>{current.v}</div>
              </div>
            )}
            {(() => {
              const grade = gradeOf(current.h, palette);
              const h = current.h;
              const Field = ({ label, value }) => (
                <div style={{ display: 'flex', gap: 10, marginBottom: 10, alignItems: 'flex-start' }}>
                  <span style={{ minWidth: 88, fontSize: 11.5, fontWeight: 800, color: palette.accent, flexShrink: 0, paddingTop: 1 }}>{label}</span>
                  <span style={{ fontSize: 13, lineHeight: 1.85, fontWeight: 600 }}>{value}</span>
                </div>
              );
              return (
                <>
                  {h.rule && (
                    <div style={{ background: `${grade.color}14`, border: `1px solid ${grade.color}45`, borderRadius: 16, padding: '13px 15px', marginBottom: 14 }}>
                      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: 7 }}>
                        <span style={{ fontSize: 11.5, fontWeight: 800, color: palette.sub }}>الحكم</span>
                        <span style={{ fontSize: 11, fontWeight: 900, color: grade.color, background: `${grade.color}24`, padding: '3px 11px', borderRadius: 999 }}>{grade.label}</span>
                      </div>
                      <div style={{ fontSize: 13.5, lineHeight: 2, fontWeight: 700 }}>{h.rule}</div>
                    </div>
                  )}
                  {h.narrator && <Field label="الراوي" value={h.narrator} />}
                  {h.scholar && <Field label="المحدث" value={h.scholar} />}
                  {h.source && <Field label="المصدر" value={h.source} />}
                  {h.page && <Field label="الصفحة أو الرقم" value={h.page} />}
                  {h.takhrij && <Field label="التخريج" value={h.takhrij} />}
                  {h.note && (
                    <div style={{ background: palette.chip, borderRadius: 16, padding: '13px 15px', fontSize: 13, lineHeight: 2, color: palette.sub, fontStyle: 'italic', marginTop: 4 }}>
                      “{h.note}”
                    </div>
                  )}
                  <div style={{ textAlign: 'center', marginTop: 18, fontSize: 11.5, color: palette.sub, fontWeight: 700 }}>
                    اللهم انفعنا بما علّمتنا، وعلّمنا ما ينفعنا
                  </div>
                </>
              );
            })()}
          </div>
        </div>
      )}
      {/* week sheet */}
      {weekOpen && (
        <div style={{ position: 'fixed', inset: 0, zIndex: 70, display: 'flex', alignItems: 'flex-end', justifyContent: 'center' }}>
          <div onClick={() => setWeekOpen(false)} style={{ position: 'absolute', inset: 0, background: 'rgba(15,18,30,.45)', animation: 'fadeIn 200ms ease', backdropFilter: 'blur(4px)' }} />
          <div style={{ position: 'relative', width: '100%', maxWidth: 480, background: isMorning ? '#FFFBF1' : '#232A45', borderRadius: '28px 28px 0 0', padding: '10px 20px 28px', maxHeight: '80vh', overflowY: 'auto', animation: 'slideUp 340ms cubic-bezier(.2,.9,.3,1.1)', fontFamily: "'Cairo', sans-serif", color: palette.ink }}>
            <div style={{ width: 42, height: 5, borderRadius: 99, background: isMorning ? 'rgba(0,0,0,.12)' : 'rgba(255,255,255,.15)', margin: '4px auto 14px' }} />
            <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}>
              <div style={{ width: 38, height: 38, borderRadius: '50%', background: palette.accentSoft, display: 'flex', alignItems: 'center', justifyContent: 'center' }}><Flame size={18} color="#E2703A" fill="#E2703A" /></div>
              <div>
                <div style={{ fontWeight: 900, fontSize: 15 }}>سجل الأسبوع</div>
                <div style={{ fontSize: 11.5, color: palette.sub, fontWeight: 600 }}>{toAr(streak)} يوم متتالٍ</div>
              </div>
              <button onClick={() => setWeekOpen(false)} style={{ marginRight: 'auto', width: 32, height: 32, borderRadius: '50%', background: palette.chip, border: 'none', cursor: 'pointer', display: 'flex', alignItems: 'center', justifyContent: 'center', color: palette.sub }}><X size={14} /></button>
            </div>
            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(7, 1fr)', gap: 8, textAlign: 'center' }}>
              {Array.from({ length: 7 }).map((_, idx) => {
                const off = 6 - idx;
                const d = new Date(); d.setDate(d.getDate() - off);
                const ds = `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;
                const hist = getHistory();
                const dayData = hist[ds];
                const done = !!(dayData && (dayData.morning || dayData.evening));
                const isToday = off === 0;
                const label = d.toLocaleDateString('ar-EG', { weekday: 'short' });
                const dayNum = d.toLocaleDateString('ar-EG', { day: 'numeric' });
                return (
                  <div key={ds} style={{ background: isToday ? palette.accentSoft : palette.chip, border: `1px solid ${isToday ? palette.accent : palette.cardBorder}`, borderRadius: 14, padding: '10px 4px' }}>
                    <div style={{ fontSize: 11, fontWeight: 700, color: palette.sub, marginBottom: 4 }}>{label}</div>
                    <div style={{ fontSize: 13, fontWeight: 900, marginBottom: 6 }}>{toAr(dayNum)}</div>
                    <div style={{ width: 22, height: 22, borderRadius: '50%', background: done ? palette.succ : 'rgba(0,0,0,0.08)', color: done ? 'white' : palette.sub, display: 'flex', alignItems: 'center', justifyContent: 'center', margin: '0 auto', fontSize: 12 }}>{done ? '✓' : '·'}</div>
                  </div>
                );
              })}
            </div>
            <div style={{ textAlign: 'center', marginTop: 14, fontSize: 11.5, color: palette.sub, fontWeight: 600 }}>اضغط خارج النافذة للإغلاق</div>
          </div>
        </div>
      )}

      {/* toast */}
      {toast && (
        <div
          key={toast.id}
          style={{
            position: 'fixed', bottom: 26, left: '50%', zIndex: 80,
            transform: 'translateX(-50%)',
            background: isMorning ? '#4A4030' : '#2E2A4E', color: '#FFF7E8',
            padding: '11px 20px', borderRadius: 999, fontSize: 13.5, fontWeight: 800,
            boxShadow: '0 12px 34px rgba(0,0,0,.28)', display: 'flex', alignItems: 'center', gap: 8,
            animation: 'toastIn 300ms cubic-bezier(.2,.9,.3,1.3)',
            fontFamily: "'Cairo', sans-serif", whiteSpace: 'nowrap', maxWidth: '92vw',
          }}
        >
          {toast.icon && <span>{toast.icon}</span>}
          <span>{toast.msg}</span>
        </div>
      )}
    </div>
  );
}
